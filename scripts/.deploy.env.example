# Deploy connection config (copy to .deploy.env and fill in)
# This file is gitignored — .deploy.env.example is tracked as a template.
DEPLOY_SSH_HOST=user@your-server-ip
DEPLOY_SSH_KEY=~/.ssh/your-key
