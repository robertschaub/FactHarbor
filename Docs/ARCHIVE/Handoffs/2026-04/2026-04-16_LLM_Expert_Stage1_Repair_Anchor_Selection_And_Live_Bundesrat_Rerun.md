---
### 2026-04-16 | LLM Expert | GitHub Copilot (GPT-5.4) | Stage 1 Repair Anchor Selection And Live Bundesrat Rerun
**Task:** Fix the remaining Bundesrat Stage 1 contract-repair failure after the earlier reruns left it at `UNVERIFIED 50/0`, then verify the result on the live local stack.
**Files touched:** `apps/web/src/lib/analyzer/claim-extraction-stage.ts`; `apps/web/test/unit/lib/analyzer/repair-anchor-selection.test.ts`; `apps/web/test/unit/lib/analyzer/claim-contract-validation.test.ts`; `Docs/AGENTS/Handoffs/2026-04-16_LLM_Expert_Stage1_Repair_Anchor_Selection_And_Live_Bundesrat_Rerun.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Added a small Stage 1 helper layer around contract repair: `claimSetContainsAnchorText(...)` centralizes case-insensitive anchor landing checks, and `selectRepairAnchorText(...)` can narrow a validator-provided repair anchor to a single still-missing salience-backed sub-anchor when that structural condition is unambiguous. Wired the repair path to use the resolved anchor and updated the existing source-inspection regression to match the helper-based implementation. Added a focused unit test file for the Bundesrat-style narrowing case.
**Validation:** File-level diagnostics were clean after editing. Focused tests passed: `claim-extraction-prompt-contract.test.ts`, `repair-anchor-selection.test.ts`, and `claim-contract-validation.test.ts` (`49/49`). The existing mocked pipeline regression `should execute the contract repair pass and accept a narrowed repair payload` also passed. `npm -w apps/web exec tsc --noEmit` passed. Local stack was restarted and re-seeded; health checks succeeded once the web app finished booting. Fresh live rerun `b92201bb47454f7498a1919c4a82c567` for `Der Bundesrat unterschrieb den EU-Vertrag rechtskräftig bevor Volk und Parlament darüber entschieden haben` no longer dies in Stage 1: SSE shows Pass 1 -> salience commitment -> preliminary search -> Pass 2 -> contract validation -> contract repair -> Gate 1 validation -> `Researching evidence for claims...` -> Stage 2 iteration 1.
**Open items:** The live SSE still logged `Repairing claim set to carry anchor "rechtskräftig bevor Volk und Parlament darüber entschieden haben" verbatim...`, so the new narrowing rule did not trigger on the actual live claim set even though the run now passes through Gate 1 and into research. After job `b92201bb47454f7498a1919c4a82c567` completes, inspect its stored `understanding.atomicClaims` and `contractValidationSummary` to see which additional missing salience span prevented narrowing and whether any further Stage 1 tightening is warranted. I did not wait for the final verdict to complete in this handoff.
**Warnings:** Local `POST /v1/analyze` now requires an invite code in the request body. The seeded `BETA-PREVIEW-2026` code was not present in the current local DB; active local codes were discovered through `GET /v1/admin/invites` with the admin key, and `TEST-CODE` was used for the rerun.
**For next agent:** Start with live job `b92201bb47454f7498a1919c4a82c567`. First read its final `resultJson.understanding.atomicClaims`, `contractValidationSummary`, and verdict once complete. If the final result is healthy, the immediate `UNVERIFIED 50/0` regression is resolved. If the final Stage 1 claim shape is still awkward, inspect why `selectRepairAnchorText(...)` kept the broad validator anchor on the live path despite the salience inventory containing `rechtskräftig` as a distinct anchor.
**Learnings:** yes — local public submissions now depend on the invite-code system; admin inventory at `v1/admin/invites` is the reliable way to find current active local codes before rerun work.
