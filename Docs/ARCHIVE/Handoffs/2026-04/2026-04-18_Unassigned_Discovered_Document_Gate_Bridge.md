### 2026-04-18 | Unassigned | Codex (GPT-5) | Discovered Document Gate Bridge
**Task:** Address the remaining review concern on Stage 2 discovered follow-ups so the top same-family document artifact is not dropped solely because its classifier input is URL-derived.
**Files touched:** `apps/web/src/lib/analyzer/research-acquisition-stage.ts`; `apps/web/test/unit/lib/analyzer/research-acquisition-stage.test.ts`
**Key decisions:** Kept the change structural and narrow. The classifier still runs across discovered follow-ups, but acquisition now guarantees one highest-priority same-family document artifact per already-relevant parent page survives the gate even when the classifier omits it. The guaranteed artifact inherits the parent relevance score as a fetch-time bridge; the remaining discovered URLs stay classifier-gated. This closes the specific “weak URL-only metadata can drop the decisive PDF” gap without introducing domain-specific rules or widening the fetch frontier beyond the existing per-parent cap.
**Open items:** No live rerun was executed in this pass. The next validation step is still a fresh exact-input run of `235000 Flüchtlinge leben in der Schweiz, das sind fast so viel im am Ende des Zweiten Weltkrieges.` on the current code so the real SEM 2025 path can be checked again.
**Warnings:** This bridge only guarantees the top document artifact per relevant parent. It does not guarantee that every discovered document artifact is fetched, and it does not change query generation, Stage 1 claim shape, or Stage 4 comparator reasoning.
**For next agent:** Start with [research-acquisition-stage.ts](/c:/DEV/FactHarbor/apps/web/src/lib/analyzer/research-acquisition-stage.ts) around the guaranteed-document logic after `classifiedByUrl`, plus the new regression in [research-acquisition-stage.test.ts](/c:/DEV/FactHarbor/apps/web/test/unit/lib/analyzer/research-acquisition-stage.test.ts). The key runtime question is whether stored miss `25dea04fb0da4ab5ad12fd5dbf76896a` is now avoided on a fresh live rerun.
**Learnings:** no
