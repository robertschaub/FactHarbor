### 2026-04-21 | Unassigned | Codex (GPT-5) | Report Review 1b52c739 Contract Retry Binding Fix
**Task:** Investigate and fix local job `1b52c73913634990b7cb99224e9d56cc` (`Was Iran actually making nukes?`) after it finished `UNVERIFIED | 50 | 0` on current HEAD.
**Files touched:** `apps/web/src/lib/analyzer/claim-extraction-stage.ts`; `apps/web/test/unit/lib/analyzer/claim-contract-validation.test.ts`
**Key decisions:** The exact requested job was inspected first, then compared only against same-input local sibling `497a712e500347d9954b0aa46b86ea55` on the same execution commit `eaacd9ce...` and same prompt hash `5a77affe...`. Root cause is Stage 1 retry fragility, not retrieval failure and not the previously fixed uncited-anchor override. The bad run decomposed the question into three historical/proxy background claims (`research and development`, `undeclared activities`, `halted work in 2003`) and the contract validator correctly rejected them because none preserved the thesis-defining anchor `"actually making nukes"`. A retry and repair were attempted, but the retry ran with audit-mode salience only, so it could still reproduce an unconstrained proxy decomposition. The fix adds a narrow structural escalation: when a contract-driven Pass 2 retry is happening, the validator reports a truth-condition anchor present in the input, and `preservedInClaimIds` is empty, the retry now upgrades from audit-mode salience to binding-mode salience so the retry extraction is forced to preserve the precommitted anchor set instead of free-drifting again.
**Open items:** I did not submit a fresh live rerun of the Iran input because source changed and the repo policy requires committed provenance before post-change live jobs. The next step is: commit, restart `apps/web`, rerun the exact input, and confirm the job follows the successful sibling pattern rather than terminating in Stage 1.
**Warnings:** This is a forward fix backed by exact-job diagnosis plus targeted local verification, not a live-rerun confirmation yet. The requested job's `resultJson.understanding.contractValidationSummary.stageAttribution` stayed `initial`, which means the retry/repair path failed to replace the original broken decomposition. The successful sibling run `497a712e500347d9954b0aa46b86ea55` shows the intended retry outcome: two thesis-direct ambiguous-dimension claims that preserve `"actually making nuclear weapons"` verbatim.
**For next agent:** The critical seam is [claim-extraction-stage.ts](/c:/DEV/FactHarbor/apps/web/src/lib/analyzer/claim-extraction-stage.ts) around the retry block and the new helper `shouldUseBindingSalienceForContractRetry(...)`. The exact bad job had `understanding.inputClassification = "question"` with 3 direct claims and `contractValidationSummary.truthConditionAnchor.preservedInClaimIds = []`; the good sibling had `inputClassification = "ambiguous_single_claim"` with 2 direct claims and both anchor carriers preserved. Verification passed on `npm -w apps/web exec vitest run test/unit/lib/analyzer/claim-contract-validation.test.ts test/unit/lib/analyzer/contract-revalidation-fallback.test.ts test/unit/lib/analyzer/claim-contract-anchor-override.test.ts` and `npx tsc -p apps/web/tsconfig.json --noEmit`.
**Learnings:** no
