### 2026-04-15 | Unassigned | Codex (GPT-5) | Skill Cross-Tool Availability
**Task:** Review `.claude/skills/docs-update` and `.claude/skills/wip-update`, then make the shared workflow skills discoverable for Codex/GPT and Gemini.
**Files touched:** `.claude/skills/docs-update/SKILL.md`, `.claude/skills/wip-update/SKILL.md`, `AGENTS.md`, `GEMINI.md`, `.gemini/skills/factharbor-agent/SKILL.md`, `factharbor-agent.skill`, `Docs/DEVELOPMENT/Claude_Code_Skills.md`, `Docs/AGENTS/README.md`, `Docs/AGENTS/Handoffs/2026-04-15_Unassigned_Skill_Cross_Tool_Availability.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Kept `.claude/skills/*` as the canonical workflow source instead of inventing separate Codex/GPT copies. Exposed all nine workflows through `AGENTS.md` and `GEMINI.md`, and added them to the Gemini helper skill so Gemini has a repo-native discovery path. Rewrote the reviewed `docs-update` and `wip-update` command examples to PowerShell/cross-tool guidance rather than leaving Bash-only examples in a Windows-first repo.
**Open items:** No functional follow-up required. If the team later introduces a real Codex/GPT repo-local skill mechanism, wire it to the same canonical `.claude/skills/*` files rather than duplicating workflow bodies.
**Warnings:** `AGENTS.md`, `GEMINI.md`, and several docs were already in a dirty working tree before this task. This change only touched the skill-discovery/doc surfaces listed above and did not normalize unrelated edits. `Docs/DEVELOPMENT/Claude_Code_Skills.md` and the two reviewed `.claude/skills/*` files are currently untracked in git state; confirm that is intentional before commit.
**For next agent:** When adding another shared workflow, update four discovery surfaces together: `AGENTS.md`, `GEMINI.md`, `.gemini/skills/factharbor-agent/SKILL.md`, and `Docs/DEVELOPMENT/Claude_Code_Skills.md`. If the Gemini helper skill changes, refresh `factharbor-agent.skill` so the packaged artifact stays aligned with the source folder.
**Learnings:** no
