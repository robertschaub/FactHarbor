### 2026-04-22 | Senior Developer | Codex (GPT-5) | SR Contract Risk Benefit Review
**Task:** Investigate the April 20 `SourceReliability` contract-hardening change, assess risks and benefits, and compare four older persisted SR evaluations against fresh reruns under the current code.
**Files touched:** `apps/web/scripts/tmp-sr-contract-risk-review.ts`; `Docs/AGENTS/Handoffs/2026-04-22_Senior_Developer_SR_Contract_Risk_Benefit_Comparison.json`; `Docs/AGENTS/Handoffs/2026-04-22_Senior_Developer_SR_Contract_Risk_Benefit_Review.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Used the live persisted SR cache in `apps/web/source-reliability.db` as the baseline source of truth and selected four pre-`403e905a` rows: `deeplearning.ai`, `siemens.com`, `accelerazero.com`, and `theglobeandmail.com`. Saved the original rows plus fresh reevaluation outputs to `Docs/AGENTS/Handoffs/2026-04-22_Senior_Developer_SR_Contract_Risk_Benefit_Comparison.json`. The comparison showed the contract-hardening patch fixes a real contract-integrity problem (unsupported `sourceType` values such as `educational_platform`, `corporate_publisher`, and `corporate_interest` no longer survive unchanged), but the current SR evaluator still has a larger semantic classification issue: several corporate/educational sources now collapse to `unknown` or are reclassified into clearly wrong canonical buckets like `collaborative_reference`, so score shifts cannot be attributed solely to the schema patch.
**Verification:** Ran a live four-domain reevaluation via `node --env-file=.env.local --import tsx .\\scripts\\tmp-sr-contract-risk-review.ts` from `apps/web`. The run completed successfully after fetching fresh evidence and produced the JSON comparison artifact. IDE lint check reported no issues for the scratch script or saved artifact.
**Open items:** The keep/remove decision should treat `403e905a` and the broader SR semantic-classification behavior separately. The contract hardening itself is worth keeping, but it should be followed by a bounded fix for canonical classification of corporate / educational / institutional sources so they do not fall into `unknown` or `collaborative_reference` inappropriately.
**Warnings:** Before/after score deltas are influenced by fresh live evidence acquisition, provider mix, and search-cache state, not only by the April 20 schema change. The strongest directly attributable signal is contract normalization of `sourceType` / `biasIndicator`, not raw score movement.
**For next agent:** Start with `Docs/AGENTS/Handoffs/2026-04-22_Senior_Developer_SR_Contract_Risk_Benefit_Comparison.json`. The most important examples are `deeplearning.ai` (old unsupported `educational_platform`, now jumps to `collaborative_reference` with a large score increase), `siemens.com` (old `corporate_publisher`, now `collaborative_reference` with similar score), and `accelerazero.com` (old `corporate_interest`, now `unknown` with slightly higher score). If you continue, isolate whether canonical mapping for corporate/educational source families belongs in prompt guidance, runtime normalization, or both.
**Learnings:** Live rerun comparisons are useful for directionally testing SR changes, but they must be interpreted through two lenses: contract-shape changes and live evidence volatility. A clean “keep/remove” call is possible here only because the patch’s direct benefit is structural and visible even when score deltas are noisy.
