### 2026-04-21 | Unassigned | Codex (GPT-5) | Benchmark Rerun Wave Five Candidates
**Task:** Submit five recent benchmark inputs with known imperfections, expectation gaps, or open challenge status; then compare the completed reports against Captain's expected bands.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-21_Unassigned_Benchmark_Rerun_Wave_Five_Candidates.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Chose exact approved benchmark inputs from `Docs/AGENTS/benchmark-expectations.json` and `Docs/AGENTS/Captain_Quality_Expectations.md`, prioritizing high-priority reruns and families with current expectation drift: `bundesrat-rechtskraftig`, `bundesrat-simple`, `asylum-235000-de`, `asylum-wwii-de`, `plastic-en`. Submitted them on committed runtime `eaacd9ce411c84a4271378502b315b5c93874e99+39837e3a` with prompt hash `5a77affe...`. The rerun wave verified the Stage 1 anchor-override fix as an early-abort repair, but it also showed that the two Bundesrat families still overshoot Captain's expected verdict/truth bands on the current stack.
**Open items:** `bundesrat-rechtskraftig` rerun `2379b10561764db98c88696ff995761c` finished `MOSTLY-TRUE | 72 | 76`, outside the expected `MIXED / LEANING-*` and truth band `35-60`. `bundesrat-simple` rerun `a6b0e0fc14984926a678a462456bc110` finished `TRUE | 97 | 89`, far outside expected bands. `asylum-wwii-de` now has a first current-stack result (`LEANING-FALSE | 38 | 65`, job `774dec9e03ae4696825c151754bc5ce7`), but the benchmark JSON still has no official band for that family. If Captain wants the benchmark expectations updated, this rerun is the candidate seed.
**Warnings:** `bundesrat-rechtskraftig` no longer collapses to `UNVERIFIED`, so the fixed Stage 1 failure mode is resolved, but the report still shows a `query_budget_exhausted` warning and over-credits the literal chronology relative to the misleading-framing expectation. `bundesrat-simple` is the strongest current quality miss in this wave; it was judged as plain factual chronology with very high confidence despite Captain's expectation that the framing should keep it in mixed/leaning territory. `plastic-en` did not regress to the old parse-failure collapse, but truth `44` remains above the expected `10-35` band even though the label `MIXED` is still allowed. `asylum-wwii-de` carried many `source_fetch_failure` infos, so its first-band candidate should be treated as useful but not final.
**For next agent:** Use these exact completed jobs for any follow-up review or expectation-update discussion: `2379b10561764db98c88696ff995761c` (`bundesrat-rechtskraftig`), `a6b0e0fc14984926a678a462456bc110` (`bundesrat-simple`), `f3fbbfd6ef6a4c2ea259dfef15857960` (`asylum-235000-de`), `774dec9e03ae4696825c151754bc5ce7` (`asylum-wwii-de`), `c7fdb65c58d744c0b364d7b7cf64c6c9` (`plastic-en`). Best clean expectation hit in this wave was `asylum-235000-de` (`LEANING-TRUE | 70 | 68`, 5 boundaries, 20 evidence items). Best challenge-value rerun was `asylum-wwii-de`, because it converts the previously unvalidated family into a concrete current-stack result. Do not treat the Bundesrat reruns as proof that the broader semantics issue is solved; they only prove the Stage 1 false-abort path is fixed.
**Learnings:** No.
