---
### 2026-04-16 | LLM Expert | GitHub Copilot (GPT-5.4) | Asylum Refinement Regression Fix
**Task:** Fix the second reported regression: the asylum approved-input run that fell from `LEANING-TRUE 62/55` to `UNVERIFIED 57/40` after the recent Stage 2 refinement changes.
**Files touched:** `apps/web/src/lib/analyzer/research-orchestrator.ts`; `apps/web/test/unit/lib/analyzer/primary-source-refinement.test.ts`.
**Key decisions:** Compared the good run `bc82574223574bb087f47276e0496907` with the regressed run `2f8cab5595cc42f0b94615b524a51c84`. The only structural delta was a new first-pass `refinement` iteration plus a `per_source_evidence_cap` warning. The current trigger refined whenever target primary-source types were still absent, even if the main iteration had already built a rich non-seeded institutional numeric evidence base. I kept the broader source-type refinement behavior from the earlier review fix, but added a guard that suppresses the extra refinement pass when a metric-bearing claim already has sufficiently rich institutional numeric coverage from the main pass.
**Open items:** Fresh live rerun of the approved asylum input is still needed to confirm the verdict returns to the pre-regression range on the current tree.
**Warnings:** This fix is intentionally narrow and only changes the refinement trigger seam. It does not alter Stage 4 verdict prompts, confidence thresholds, or the earlier Stage 1 contract-repair work. I did not run a full `claimboundary-pipeline.test.ts` sweep because that file still has the known unrelated Stage 2 query-count failure at `test/unit/lib/analyzer/claimboundary-pipeline.test.ts:3526`.
**For next agent:** The key evidence for the regression is in the two stored local jobs: `bc82574223574bb087f47276e0496907` had no refinement iteration and finished `LEANING-TRUE 62/55`; `2f8cab5595cc42f0b94615b524a51c84` added one refinement iteration with `laneReason=primary_source_refinement:recovered_non_seeded_primary_coverage`, hit `per_source_evidence_cap`, and fell to `UNVERIFIED 57/40`. The new guard lives in `claimNeedsPrimarySourceRefinement(...)`.
**Learnings:** no — repo-specific behavior was documented in this handoff instead of Role_Learnings.
