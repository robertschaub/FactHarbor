---
### 2026-04-23 | Unassigned | Codex (GPT-5) | Claim Selection Dialog Slow Path Diagnosis
**Task:** Monitor the local application and logs to determine why checkworthiness detection inside the claim-selection dialog is slow.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-23_Unassigned_Claim_Selection_Dialog_Slow_Path_Diagnosis.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** The current slowdown is not primarily the post-Stage-1 claim-selection recommendation call. The draft worker in `apps/web/src/lib/internal-runner-queue.ts` waits for full `prepareStage1Snapshot(...)` completion before it even starts recommendation at progress `32`. The live draft `2698064e48b446aa8b6a7d69d40ce504` for `https://en.wikipedia.org/wiki/Iran_and_weapons_of_mass_destruction` remained stuck at progress `24` for more than 200 seconds while Stage 1 was still running, and it only reached `AWAITING_CLAIM_SELECTION` after about `463.8s` total with `25` candidate claims and `3` recommended claims. Local server logs showed the concrete bottleneck: claim-contract validation returned no usable structured result after two attempts, then Stage 1 reran Pass 2 with conservative contract guidance, revalidated that retry cleanly, preserved all 25 claims through Gate 1, and only then moved into the recommendation step. Historical local jobs for the same Iran URL also show this input class is intrinsically expensive in Stage 1 (`understand` phase observed at ~240s, ~363s, and ~512s). The perceived freeze is amplified by sparse progress reporting: initial contract validation uses progress `24`, retry Pass 2 also reuses progress `24`, and recommendation progress `32` is not emitted until Stage 1 fully returns.
**Open items:** If the goal is optimization rather than diagnosis, the next work should instrument separate draft-prep timings for URL fetch, preliminary search, Pass 2, contract validation, retry Pass 2, retry revalidation, repair pass, and recommendation. After that, decide whether to cache prepared Stage 1 snapshots for identical inputs, reduce Stage 1 work in the ACS draft path, or improve progress messaging first.
**Warnings:** The dialog can look frozen even while work continues, because the retry/revalidation path does not emit meaningful intermediate progress updates to the draft row. Also note that the recommendation step itself is still non-trivial: `generateClaimSelectionRecommendation(...)` serializes the full `atomicClaims` payload and uses the `context_refinement` route, which currently resolves to `config.modelVerdict`.
**For next agent:** Start with [internal-runner-queue.ts](/c:/DEV/FactHarbor/apps/web/src/lib/internal-runner-queue.ts:826), [claimboundary-pipeline.ts](/c:/DEV/FactHarbor/apps/web/src/lib/analyzer/claimboundary-pipeline.ts:541), [claim-extraction-stage.ts](/c:/DEV/FactHarbor/apps/web/src/lib/analyzer/claim-extraction-stage.ts:341), [claim-extraction-stage.ts](/c:/DEV/FactHarbor/apps/web/src/lib/analyzer/claim-extraction-stage.ts:423), and [claim-selection-recommendation.ts](/c:/DEV/FactHarbor/apps/web/src/lib/analyzer/claim-selection-recommendation.ts:227). The critical live evidence from `apps/web/.next/dev/logs/next-development.log` was: `Claim contract validation returned no usable result after two attempts.`, `Retrying Pass 2 with conservative contract guidance.`, `Claim contract retry produced 25 claim(s).`, `Claim contract retry validated cleanly; using retry Pass 2.`, and later the draft row finally moved to `AWAITING_CLAIM_SELECTION` at progress `32`.
**Learnings:** no — not appended to `Docs/AGENTS/Role_Learnings.md`.
