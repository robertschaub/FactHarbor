---
### 2026-04-15 | Unassigned | Codex (GPT-5) | ClaimBoundary Prompt UCM Propagation Review
**Task:** Review runtime/UCM risk around the recent `claimboundary.prompt.md` fix and determine the next step to ensure the corrected prompt reaches target environments.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-15_Unassigned_ClaimBoundary_Prompt_UCM_Propagation_Review.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Runtime prompt resolution is DB-first via `config_active`/`config_blobs`, not file-first. System-seeded prompts can self-refresh from disk, but manually edited/admin-activated prompts do not. Current local `apps/web/config.db` still shows active `claimboundary` as `seed-v1.0.0` (`system-seed`) while `apps/web/prompts/claimboundary.prompt.md` is `1.0.1`, so the file fix has not yet been materialized into local UCM state. The safest next step is per-environment verification of the active prompt metadata, then environment-specific propagation: CLI reseed for system-seeded envs, manual compare/import/activate for admin-managed envs.
**Open items:** Verify each target environment’s active `prompt/claimboundary` metadata (`createdBy`, `versionLabel`, `contentHash`, `activatedBy`). Confirm whether deployment build-time `postbuild` writes to the same DB path the runtime reads (`FH_CONFIG_DB_PATH` / `config.db`). After rollout, verify a fresh job records the expected prompt hash.
**Warnings:** The Admin prompt “Load from File” path calls force reseed as the current admin user, which converts a `system-seed` prompt into an admin-owned prompt; future automatic refreshes from disk then stop applying. Prompt drift is not surfaced by the Admin “effective/default comparison” view, so prompt environments can diverge silently unless operators inspect active metadata/history directly.
**For next agent:** Check `apps/web/prompts/claimboundary.prompt.md`, `apps/web/src/lib/config-loader.ts`, `apps/web/src/lib/config-storage.ts`, `apps/web/src/lib/analyzer/prompt-loader.ts`, `apps/web/scripts/reseed-all-prompts.ts`, `apps/web/src/app/api/admin/config/[type]/[profile]/reseed/route.ts`, `apps/web/src/app/api/admin/config/[type]/[profile]/route.ts`, `apps/web/src/app/api/admin/config/default-comparison/route.ts`, `apps/web/src/app/admin/config/page.tsx`, and `apps/web/src/app/api/fh/jobs/[id]/prompts/route.ts`. Local DB inspection already showed active `claimboundary` prompt metadata at `seed-v1.0.0` / `system-seed`; use that only as local evidence, not as proof for other environments.
**Learnings:** No
