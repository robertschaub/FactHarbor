### 2026-04-19 | Unassigned | Codex (GPT-5) | Inverse Verification And SR Contract Hardening
**Task:** Execute the reduced no-regret subset from `Docs/WIP/2026-04-19_Consolidated_Prompt_Audit_Implementation_Plan.md`: harden inverse-claim verification (`P1`) and apply narrow source-reliability schema tightening (`P2`), then verify safely.
**Files touched:** `apps/web/prompts/text-analysis/inverse-claim-verification.prompt.md`; `apps/web/src/lib/calibration/paired-job-audit.ts`; `apps/web/test/unit/lib/paired-job-audit.test.ts`; `apps/web/src/lib/source-reliability/sr-eval-types.ts`; `apps/web/src/lib/source-reliability/sr-eval-engine.ts`; `apps/web/src/lib/source-reliability/sr-eval-prompts.ts`; `apps/web/test/unit/lib/source-reliability/sr-eval-types.test.ts`; `Docs/AGENTS/Handoffs/2026-04-19_Unassigned_Inverse_Verification_And_SR_Contract_Hardening.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** `P1` was implemented as prompt-plus-parser hardening, not as a wider calibration API redesign. `inverse-claim-verification.prompt.md` now defines strict inverse boundaries explicitly for quantifiers, modality, scope, timeframe, identical claims, empty claims, and cross-language comparisons. `paired-job-audit.ts` now validates the verifier JSON with Zod, extracts fenced/raw JSON defensively, short-circuits empty-claim inputs, and degrades to `isStrictInverse: false` with diagnostic reasoning instead of throwing when the verifier path fails. `P2` was kept narrow: `sr-eval-types.ts` now canonicalizes `sourceType`, `politicalBias`, and `otherBias` to explicit enums, preserves one known legacy alias path (`political_party` -> `advocacy`), and defaults unsupported values to safe canonical fallbacks instead of letting free strings drift into the runtime. `sr-eval-engine.ts` now preserves canonical `biasIndicator` tokens in the response payload, and the live few-shot example in `sr-eval-prompts.ts` was corrected to stop teaching the off-contract `political_party` / `otherBias: "advocacy"` output.
**Verification:** Focused safe tests passed with `npm -w apps/web exec vitest run test/unit/lib/paired-job-audit.test.ts test/unit/lib/source-reliability/sr-eval-types.test.ts test/unit/app/api/internal/evaluate-source/evaluator-logic.test.ts` (`33 passed`). Full web build passed with `npm -w apps/web run build`, including the repo’s postbuild prompt reseed step (`Configs: 0 changed, 4 unchanged | Prompts: 0 changed, 3 unchanged | Errors: 0`).
**Open items:** The optional diagnostic-only `P4`/`P5` work was intentionally not taken in this turn to keep blast radius low. The markdown `apps/web/prompts/source-reliability.prompt.md` remains a secondary non-runtime surface; the live SR engine still builds prompts from `sr-eval-prompts.ts`, so broader prompt-surface unification is still a separate follow-up.
**Warnings:** `apps/web/prompts/claimboundary.prompt.md` and `apps/web/test/unit/lib/analyzer/claim-extraction-prompt-contract.test.ts` were already dirty before this work and were left untouched. This handoff covers only the inverse-verification and SR contract files listed above.
**For next agent:** Start with `apps/web/src/lib/calibration/paired-job-audit.ts` and `apps/web/src/lib/source-reliability/sr-eval-types.ts`. The inverse verifier now fails soft instead of throwing, and SR evaluator outputs are canonicalized before post-processing. If you continue this track, the next bounded step is either `P4` extraction-coercion observability or the larger SR prompt-surface unification decision; do not mix that with unrelated `claimboundary` edits in the already-dirty worktree.
**Learnings:** no

