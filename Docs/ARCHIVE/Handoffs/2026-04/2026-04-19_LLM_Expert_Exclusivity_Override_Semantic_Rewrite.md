---
### 2026-04-19 | LLM Expert | Codex (GPT-5) | Exclusivity Override Semantic Rewrite
**Task:** Replace the interim token-shaped `Exclusivity/uniqueness override` wording in `apps/web/prompts/claimboundary.prompt.md` with the reviewed semantic-structure version, then verify the paired prompt-contract test and prompt activation state.
**Files touched:** `apps/web/prompts/claimboundary.prompt.md`, `apps/web/test/unit/lib/analyzer/claim-extraction-prompt-contract.test.ts`, `Docs/AGENTS/Handoffs/2026-04-19_LLM_Expert_Exclusivity_Override_Semantic_Rewrite.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** The final prompt wording now describes the exclusivity structure semantically instead of relying on English lexical cues (`sole`/`only`/`unique`). It explicitly says to extract at least the subject-specific proposition plus the comparison-class exclusivity proposition, allows more than two claims only when the input itself carries further independently verifiable propositions, forbids creating extra claims merely because verification uses multiple evidence routes, and keeps the anti-regression safeguard that decomposed claims must be genuine sub-assertions rather than verbatim copies of the full input. The ordering guard was also softened after review: it now blocks bad decomposition for temporal/ordinal/rank cases without hard-forcing `single_atomic_claim`, and instead returns those inputs to the normal classification rules unless the input itself contains an additional independent assertion.
**Open items:** The latest prompt/test changes are local only — not committed. Optional but still useful: one fresh live rerun of the motivating Swiss input to confirm the new wording produces the intended atomic-claim split at runtime, not just in prompt-contract tests.
**Warnings:** The repo has unrelated dirty/untracked docs/index/handoff files in the worktree; do not sweep them into a commit with the prompt change. Verification here is prompt-contract plus prompt reseed/activation only; no fresh analysis job was run in this final semantic-rewrite step.
**For next agent:** Current anchors are `apps/web/prompts/claimboundary.prompt.md:72-73` and `:222-223` for the final semantic exclusivity + ordering guard, and `apps/web/test/unit/lib/analyzer/claim-extraction-prompt-contract.test.ts:208-231` for the focused contract assertions. Verification passed via `npm -w apps/web run test -- test/unit/lib/analyzer/claim-extraction-prompt-contract.test.ts test/unit/lib/analyzer/prompt-frontmatter-drift.test.ts`. Prompt reseed updated and activated `claimboundary` to hash `8298884f5ede80863fe2a9195cfb4d33aec0aa86f26c0b1c1f8f8f3a5127ff0f` (`activated_utc=2026-04-19T12:15:33.099Z`), so future report generation on this machine should use the new wording.
**Learnings:** no

