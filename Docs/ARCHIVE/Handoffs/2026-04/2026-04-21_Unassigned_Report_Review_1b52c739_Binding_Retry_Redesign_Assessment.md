---
### 2026-04-21 | Unassigned | Codex (GPT-5) | Report Review 1b52c739 Binding Retry Redesign Assessment
**Task:** Re-investigate the uncommitted Stage 1 retry-binding patch after expert review findings and identify a better structural solution.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-21_Unassigned_Report_Review_1b52c739_Binding_Retry_Redesign_Assessment.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** The reviewer findings are valid. The current helper `shouldUseBindingSalienceForContractRetry(...)` is the wrong abstraction because it decides from the raw validator payload and the original salience inventory. The better fix is to replace it with a builder that uses the evaluated contract result (`validPreservedIds`, not raw `preservedInClaimIds`) and, when retry escalation is justified, constructs a merged binding inventory that includes the validator-discovered truth-condition anchor if the original salience set does not already contain that exact anchor text. This keeps binding mode truthful to the prompt contract, which treats the provided anchor list as authoritative in binding mode.
**Open items:** The redesign is analyzed but not yet implemented. The remaining design choice to pressure-test with Claude is whether synthetic anchor-only binding should ever be allowed when the upstream salience stage returned no usable anchors; current recommendation is no, because the prompt explicitly frames binding mode as upstream precommitment.
**Warnings:** The current uncommitted patch is not safe to ship as-is. Reviewer P1 is real because `CLAIM_EXTRACTION_PASS2_BINDING_APPENDIX` and `CLAIM_CONTRACT_VALIDATION_BINDING_APPENDIX` both state that the provided anchor list is the sole authoritative inventory in binding mode. Reviewer P2 is also real because raw non-empty `preservedInClaimIds` can still collapse to zero structurally valid anchor carriers.
**For next agent:** Replace the boolean helper in `apps/web/src/lib/analyzer/claim-extraction-stage.ts` with a binding-context builder driven by `EvaluatedClaimContractValidation.summary.truthConditionAnchor.validPreservedIds`. Add tests for: (1) non-empty but invalid carrier IDs still escalating, (2) salience inventory missing the validator anchor and the merged binding list gaining it, (3) valid carrier present so no escalation, and (4) no successful salience commitment so no merged binding is created. Relevant prompt anchors are `CLAIM_EXTRACTION_PASS2_BINDING_APPENDIX` and `CLAIM_CONTRACT_VALIDATION_BINDING_APPENDIX` in `apps/web/prompts/claimboundary.prompt.md`.
**Learnings:** no

