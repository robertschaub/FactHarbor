### 2026-04-22 | Senior Developer / DevOps Expert | GitHub Copilot (GPT-5.4) | Runner Admin Reads For Hidden Jobs
**Task:** Fix the deployed runner failure mode where hidden jobs could be marked RUNNING but then fail before Stage 1 because the web runner fetched job detail/list data through public `/v1/jobs` reads without admin auth.
**Files touched:** `apps/web/src/lib/internal-runner-queue.ts`; `apps/web/test/unit/lib/drain-runner-pause.integration.test.ts`; `Docs/AGENTS/Handoffs/2026-04-22_Senior_Developer_DevOps_Expert_Runner_Admin_Reads_For_Hidden_Jobs.md`; `Docs/AGENTS/Agent_Outputs.md`; `Docs/AGENTS/Role_Learnings.md`
**Key decisions:** Kept the fix on the owning abstraction instead of changing API semantics. `internal-runner-queue.ts` now builds admin headers once and threads `X-Admin-Key` through every runner-owned `GET /v1/jobs...` call: execution-start job load, late-status guard read, queue recovery list read, orphan refresh read, and dequeued job re-read. That closes both the immediate hidden-job execution failure and the same auth gap in restart/recovery paths. Added a focused runner integration test that proves the queue list read and the job detail reads all carry the admin key during a drain cycle.
**Open items:** Deploy or restart the web service that owns the runner so the patched helper is live, then retry the exact failed asylum job. I did not perform the deployed retry from this workspace.
**Warnings:** The runner still depends on `FH_ADMIN_KEY` being configured in the web environment. This patch forwards the key consistently when present; it does not add a new startup hard-fail for missing configuration.
**For next agent:** The regression test is in `apps/web/test/unit/lib/drain-runner-pause.integration.test.ts`. If the retried deployed job still fails, inspect the deployed web env first: confirm `FH_ADMIN_KEY` is set there and matches the API’s `Admin:Key`, then verify the service was restarted after rollout. The runtime code path to watch is `drainRunnerQueue()` / `runJobBackground()` in `apps/web/src/lib/internal-runner-queue.ts`.
**Learnings:** yes — appended a Senior Developer learning covering runner-owned read paths and hidden/admin semantics.
