### 2026-04-17 | Senior Developer | Codex (GPT-5) | Search Cache Review Followups
**Task:** Address the current review follow-ups on search-cache fingerprinting, `/prompt-audit` wording hygiene, `.claude/settings.json` formatting, and the still-open SSE rate-limit gap.
**Files touched:** `apps/web/src/lib/search-cache.ts`; `apps/web/test/unit/lib/search-cache.test.ts`; `.claude/skills/prompt-audit/SKILL.md`; `.claude/settings.json`; `apps/api/Controllers/JobsController.cs`
**Key decisions:** Search-cache fingerprint fallbacks now read from `DEFAULT_SEARCH_CONFIG` instead of duplicating defaults inline; cache tests now cover detected language, configured provider, and implicit-vs-explicit default equivalence; the `/prompt-audit` R2 threshold note was clarified as a skill-local audit band rather than an analyzer runtime tuning recommendation; `.claude/settings.json` was normalized for indentation only; `JobsController.EventsSse` now uses `ReadPerIp` rate limiting like the other public read routes.
**Open items:** Pre-fix search-cache rows remain unreachable until their normal TTL expiry; the prior-review Stage 1 carry-forwards (`enforceFreshnessContract` relabel test coverage and retry-path `filterByCentrality(...)` anchor-carrier threading) are still open; the historical commit-subject convention issue on `93fd0fce` was not changed because that would require history rewrite.
**Warnings:** `npm -w apps/web run build` triggers prompt/config reseeding in `postbuild`; generated index files under `Docs/AGENTS/index/` were already dirty before this task and should be treated as generated output, not hand-edited review scope.
**For next agent:** If you continue the review backlog, the next concrete code area is `apps/web/src/lib/analyzer/claim-extraction-stage.ts` around the retry-path `filterByCentrality(...)` calls and the freshness-contract relabel path. If the orphaned cache rows matter operationally, handle that as a deploy/cache-clear step rather than by further changing the cache key logic.
**Learnings:** no
