### 2026-04-18 | Senior Developer | Codex (GPT-5) | Asylum WWII Discovered Follow-Up Relevance Gating
**Task:** Address the Lead Developer re-review concern that Stage 2 discovered follow-up URLs were bypassing relevance gating and entering acquisition as an unscored side lane.
**Files touched:** `apps/web/src/lib/analyzer/research-acquisition-stage.ts`; `apps/web/src/lib/analyzer/research-orchestrator.ts`; `apps/web/src/lib/analyzer/types.ts`; `apps/web/test/unit/lib/analyzer/research-acquisition-stage.test.ts`; `Docs/AGENTS/Handoffs/2026-04-18_Senior_Developer_Asylum_WWII_Discovered_Followup_Relevance_Gating.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Kept the accepted multi-hop retrieval shape and depth bounds intact. Instead of shrinking discovery, threaded discovered same-family follow-ups back through the existing Haiku-based `classifyRelevance` path before fetch, then re-applied per-parent selection with `selectTopSources(..., MAX_DISCOVERED_FOLLOW_UPS_PER_SOURCE)`. Added minimal structural metadata (`title`, parent-derived `snippet`) for discovered candidates so the classifier has context without domain hardcoding. Also persisted `relevanceScore` onto `FetchedSource` so newly fetched discovered documents no longer become unscored once they enter `state.sources`. Wired the same callback into both the main Stage 2 lane and the EN supplementary lane.
**Open items:** No fresh live rerun was performed on the approved asylum/WWII input after this re-gating patch. The previously isolated Stage 4 historical-comparator issue remains the user-visible blocker and should be validated separately on the next live run. Concern B from the review (`[data-url]`, `/par.html`, `.entries.html` in `retrieval.ts`) remains accepted structural plumbing for now and was intentionally left unchanged.
**Warnings:** The discovery lane now respects relevance gating, but it still synthesizes lightweight titles/snippets from URL structure plus the already-relevant parent page. That is intentional structural plumbing, not semantic hardcoding, but if a future reviewer wants richer discovered-source metadata it should still come from generic extraction rather than CMS-specific heuristics. Existing pipeline tests emit their usual mocked metrics/LLM fallback stderr noise; verification remained green.
**For next agent:** The key code anchors are `apps/web/src/lib/analyzer/research-acquisition-stage.ts` around `collectDiscoveredFollowUps(...)`, the new `classifyDiscoveredSources` option, and the persisted `relevanceScore` on `FetchedSource`; plus `apps/web/src/lib/analyzer/research-orchestrator.ts` where both fetch call sites now pass discovered candidates back through `classifyRelevance(...)`. Validate next with the Captain-approved asylum/WWII input and compare against stored job `23d05e2f16d9493d9a2a37a215d9813c`. Expected outcome of this patch is narrower Stage 2 fetch fan-out with the decisive SEM PDF still reachable; it does not attempt to solve the separate Stage 4 comparator verdicting issue.
**Learnings:** no
