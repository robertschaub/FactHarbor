---
### 2026-04-21 | Unassigned | Codex (GPT-5) | Report Review 1b52c739 Retry Salience Plan Fix
**Task:** Replace the unsafe raw-payload retry-binding helper for job `1b52c73913634990b7cb99224e9d56cc` with a structurally correct retry salience plan after expert review findings.
**Files touched:** `apps/web/src/lib/analyzer/claim-extraction-stage.ts`; `apps/web/test/unit/lib/analyzer/claim-contract-validation.test.ts`; `Docs/AGENTS/Handoffs/2026-04-21_Unassigned_Report_Review_1b52c739_Retry_Salience_Plan_Fix.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Removed `shouldUseBindingSalienceForContractRetry(...)` and replaced it with `buildContractRetrySaliencePlan(...)`, which is driven by the evaluated contract result (`effectiveRePromptRequired` plus `truthConditionAnchor.validPreservedIds`) instead of the raw validator payload. When a truth-condition anchor is present in the input but has zero structurally valid carriers, retry now behaves in one of two ways: (1) if upstream salience produced a trustworthy anchor inventory, retry upgrades to binding mode and prepends a merged validator-discovered anchor to that inventory; (2) if no trustworthy upstream inventory exists, retry stays in audit mode and relies on corrective guidance only. The old mutation of `contractResult.inputAssessment.rePromptRequired` was removed; retry gating now reads `evaluatedContract?.effectiveRePromptRequired` directly.
**Open items:** I did not add new observability fields for retry escalation mode or anchor text. If Captain wants deeper post-hoc diagnostics for this failure family, the next small slice is to persist `binding_merged_anchor` vs `audit_guidance_only` in `contractValidationSummary`.
**Warnings:** The merged anchor currently defaults to salience type `action_predicate` so it participates in the existing `priorityAnchors` path without widening the stored salience type contract. That is an operational choice, not a claim that the validator can always recover full grammatical anchor typing. No live rerun was executed after the code fix.
**For next agent:** The important runtime seams are [claim-extraction-stage.ts](/c:/DEV/FactHarbor/apps/web/src/lib/analyzer/claim-extraction-stage.ts) around the initial contract-validation block and the new `buildContractRetrySaliencePlan(...)` helper, plus the replacement tests in [claim-contract-validation.test.ts](/c:/DEV/FactHarbor/apps/web/test/unit/lib/analyzer/claim-contract-validation.test.ts). Verification passed on `npm -w apps/web exec vitest run test/unit/lib/analyzer/claim-contract-validation.test.ts test/unit/lib/analyzer/contract-revalidation-fallback.test.ts test/unit/lib/analyzer/claim-contract-anchor-override.test.ts` and `npx tsc -p apps/web/tsconfig.json --noEmit`. The remaining product-level step is a fresh rerun of the Iran input on committed runtime.
**Learnings:** no

