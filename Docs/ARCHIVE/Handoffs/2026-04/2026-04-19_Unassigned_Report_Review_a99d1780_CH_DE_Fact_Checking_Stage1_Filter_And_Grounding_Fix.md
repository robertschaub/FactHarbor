---
### 2026-04-19 | Unassigned | Codex (GPT-5) | Report Review a99d1780 CH DE Fact-Checking Stage1 Filter And Grounding Fix
**Task:** Analyze job `a99d17807c1c47dea23270bc8b1880b3` for input `Die Schweiz hat kein systematisches Fact-Checking wie Deutschland`, debate the root cause, implement the fix, and run a review on the fix.
**Files touched:** apps/web/src/lib/analyzer/claim-extraction-stage.ts; apps/web/prompts/claimboundary.prompt.md; apps/web/test/unit/lib/analyzer/claimboundary-pipeline.test.ts; Docs/AGENTS/Handoffs/2026-04-19_Unassigned_Report_Review_a99d1780_CH_DE_Fact_Checking_Stage1_Filter_And_Grounding_Fix.md; Docs/AGENTS/Agent_Outputs.md
**Key decisions:** Confirmed this is not prompt-rollout drift: the job and active `claimboundary` prompt both used hash `626f17c05a63788d47acc65b444a1865bff077395f512d4e5d1d9d019f31ccd9`. The debate converged on a two-layer failure: (1) Stage 1 preliminary search seeded off-target institutional pages before any relevance screen ran; (2) the prompt still allowed lexical-overlap institutional sources and unsupported background knowledge to leak into ecosystem reasoning. Implemented an upstream code fix by running `classifyRelevance()` inside `runPreliminarySearch()` before fetch/extraction and wiring Pass-1 geography into that call. Hardened the prompt to keep the named activity explicit in institutional-comparison queries, reject different-sector lexical overlaps, forbid omission-based evidence from unrelated sources, and forbid out-of-record ecosystem assertions in verdict reasoning.
**Open items:** The prompt change is on disk but not confirmed live on the running stack because no prompt reseed / live rerun was executed in this turn. Residual risk remains that Stage-1 preliminary relevance still lacks full `expectedEvidenceProfile` context because rough claims do not carry it pre-Pass-2; the new screen is still a meaningful upstream guard but not a full substitute for later Stage-2 claim-aware relevance.
**Warnings:** A fix review found one real issue in the first draft: Pass-1 geography was not forwarded into the new preliminary relevance gate. That was corrected and covered with a focused test. I did not run `npm -w apps/web run build` because `postbuild` reseeds prompts and mutates runtime config state. I also did not run a fresh live job against `localhost:3000` because the report-review workflow requires clean analyzer/prompt/config paths before reseed/validate.
**Verification:** Passed `npm -w apps/web run test -- test/unit/lib/analyzer/claimboundary-pipeline.test.ts -t "Stage 1: runPreliminarySearch"`; passed `npm -w apps/web run test -- test/unit/lib/analyzer/claimboundary-pipeline.test.ts -t "forwards pass1 detectedLanguage and inferredGeography into preliminary relevance classification"`; passed `npm -w apps/web run test -- test/unit/lib/analyzer/claim-extraction-prompt-contract.test.ts test/unit/lib/analyzer/verdict-prompt-contract.test.ts test/unit/lib/analyzer/prompt-frontmatter-drift.test.ts`.
**For next agent:** If Captain wants live confirmation, reseed prompts, restart `apps/web`, then rerun the exact input and compare the new result against `a99d17807c1c47dea23270bc8b1880b3` and `e8777ef27ee74b649f80daf11d22ddcf`. Pay specific attention to whether preliminary sources like DEval/BMG/EDK/IdéeSport disappear before extraction and whether verdict reasoning stops referencing uncited German fact-checking examples.
**Learnings:** The first upstream filter patch was directionally right but incomplete until Pass-1 geography was threaded through. For institutional-comparison failures, review the Stage-1 handoff path before assuming prompt-only fixes are sufficient.
