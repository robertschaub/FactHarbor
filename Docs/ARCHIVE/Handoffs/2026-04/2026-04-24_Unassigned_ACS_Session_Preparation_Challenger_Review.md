---
### 2026-04-24 | Unassigned | Codex (GPT-5) | ACS Session Preparation Challenger Review
**Task:** Review three proposed ACS/session-preparation changes from the Challenger side and identify unsafe scope, concrete risks, and acceptance conditions.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-24_Unassigned_ACS_Session_Preparation_Challenger_Review.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Block cross-session prepared-snapshot reuse for identical public URLs in the current slice. Current `PreparedStage1Snapshot` persists only `resolvedInputText` + `preparedUnderstanding`, while Stage 1 semantics still depend on live URL fetch plus live preliminary search/evidence-seeded refinement, so exact URL equality is not a semantics-complete reuse contract. Narrowly allow only log-attribution cleanup and truthful UI wording refinement because both can ship without changing Stage 1 behavior or weakening the per-draft token boundary.
**Open items:** Decide whether to implement only changes 2 and 3 now. If cross-session reuse is reopened later, it needs separate correctness/privacy approval plus a stronger persisted provenance/freshness contract before any server-side shared cache is introduced.
**Warnings:** The active plan already sequences safer work first: reduce Stage 1 time-to-selection under current semantics, then handle observability. Recommendation is downstream of Stage 1 and is documented as not the main delay driver, so wording changes must explain sequencing without implying recommendation is the root latency problem. The current draft/session surface is intentionally same-browser and per-draft-token scoped; any shared reuse pool would change that security model.
**For next agent:** If shipping now, keep scope to [internal-runner-queue.ts](/c:/DEV/FactHarbor/apps/web/src/lib/internal-runner-queue.ts), [debug.ts](/c:/DEV/FactHarbor/apps/web/src/lib/analyzer/debug.ts), [page.tsx](/c:/DEV/FactHarbor/apps/web/src/app/analyze/select/[draftId]/page.tsx), and [page-helpers.ts](/c:/DEV/FactHarbor/apps/web/src/app/analyze/select/[draftId]/page-helpers.ts). Do not add cross-draft snapshot stores or broaden token/session reach beyond the existing per-draft access model in [claim-selection-draft-proxy.ts](/c:/DEV/FactHarbor/apps/web/src/lib/claim-selection-draft-proxy.ts) and [ClaimSelectionDraftsController.cs](/c:/DEV/FactHarbor/apps/api/Controllers/ClaimSelectionDraftsController.cs).
**Learnings:** no
