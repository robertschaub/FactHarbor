### 2026-04-19 | Unassigned | Codex (GPT-5) | Bundesrat Repair Anchor Narrowing Fix
**Task:** Apply the next targeted forward fix for the Captain-defined Bundesrat input after clean job `cb2361aa346d4e9583819ccde37598d7` failed with `UNVERIFIED 50/0`, then verify it on a fresh exact-input local rerun.
**Files touched:** `apps/web/src/lib/analyzer/claim-extraction-stage.ts`; `apps/web/test/unit/lib/analyzer/repair-anchor-selection.test.ts`; `Docs/AGENTS/Handoffs/2026-04-19_Unassigned_Bundesrat_Repair_Anchor_Narrowing_Fix.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** The already-documented `MT-5(C)` skip fix was confirmed present and regression-tested on current `HEAD`; it was not the remaining seam. The clean failing job `cb2361aa...` showed a different Stage-1 path: contract validation -> repair -> Gate 1 -> `report_damaged`, with the persisted repair summary carrying anchor `bevor Volk und Parlament darüber entschieden haben` and an invalid whole-claim-plus-`Volk` branch set. The narrow forward fix was to change `selectRepairAnchorText(...)` in `claim-extraction-stage.ts` so it only narrows to modifier/predicate-like salience spans (`modal_illocutionary`, `action_predicate`) and keeps the validator's broader anchor when the lone still-missing narrowed span is temporal/scope-shaped. Added a focused regression at `repair-anchor-selection.test.ts:133` for the exact Bundesrat-shaped case where the claim already contains `rechtskräftig` but is missing the temporal clause; expected behavior is now to keep the full validator anchor instead of narrowing to the temporal sub-anchor.
**Open items:** The fresh rerun `e26048eb15b042f5ba9f0b42a59e35c3` succeeded (`LEANING-TRUE 62.9 / 73`) and cleared the Stage-1 hard-failure path, but the stored `understanding.contractValidationSummary` still notes a decomposition-integrity concern: `AC_01` is the whole proposition and `AC_02` still isolates the `Volk` branch. This is no longer a `report_damaged` blocker, but it is still a quality/calibration seam if Captain wants the family tightened further.
**Warnings:** Local runtime verification was done against the currently running stack on executed build `88126439ecbad5c6851a7b677746e2a46cbecd55+5814ca8f`, not the older clean baseline `fc4c657f...`. The new run proves the Stage-1 collapse is unblocked on current code, not that the Bundesrat family is fully solved in the benchmark sense.
**For next agent:** Start with `apps/web/src/lib/analyzer/claim-extraction-stage.ts:2777-2800` and `apps/web/test/unit/lib/analyzer/repair-anchor-selection.test.ts:133-146`. Verification anchors are failing clean job `cb2361aa346d4e9583819ccde37598d7` (`UNVERIFIED 50/0`, repair anchor narrowed to temporal clause) versus fresh successful rerun `e26048eb15b042f5ba9f0b42a59e35c3` (`LEANING-TRUE 62.9 / 73`, Stage 1 cleared into research/verdict). If more tightening is needed, inspect whether contract repair should additionally reject whole-claim-plus-subclaim outputs even when the broader anchor is preserved.
**Learnings:** no
