---
### 2026-04-24 | Senior Developer / LLM Expert | Codex | Pipeline Integrity Fixes
**Task:** Investigate the live SVP PDF run, fix issues seen on the way, and improve analysis pipeline quality first.
**Files touched:** `apps/web/src/lib/analyzer/verdict-stage.ts`, `apps/web/test/unit/lib/analyzer/verdict-citation-sanitization.test.ts`, `apps/web/src/lib/claim-selection-flow.ts`, `apps/web/src/app/analyze/select/[draftId]/page.tsx`, `apps/web/test/unit/lib/claim-selection-flow.test.ts`.
**Key decisions:** Final citation integrity now downgrades directional verdicts to `UNVERIFIED` when sanitation leaves the decisive citation side empty. The downgrade is limited to the 7-band directional ranges so valid `MIXED`/`UNVERIFIED` midpoint results are not punished. Claim-selection seeding now prefers current recommendations until there is evidence of real user interaction, preventing seeded/stale `selectedClaimIds` from masking recommended claims.
**Open items:** The old completed SVP job still contains the pre-fix AC_04 issue; rerun a live job after committing/restarting if Captain wants live validation. ReportMarkdown remains a stub and was not addressed in this slice.
**Warnings:** The first validation attempt overreached by downgrading near-midpoint repaired verdicts; that was narrowed after `verdict-stage.test.ts` failed. Do not broaden the final citation guard back to `truthPercentage > 50` / `< 50`; use the 7-band directional ranges.
**For next agent:** Relevant helpers are `enforceVerdictCitationIntegrity`, `missingCurrentDecisiveCitationSide`, `safeDowngradeCollapsedCitationVerdicts`, and `resolveInitialClaimSelection`. Focused verification passed: `npm -w apps/web run test -- test/unit/lib/claim-selection-flow.test.ts test/unit/lib/analyzer/verdict-citation-sanitization.test.ts test/unit/lib/analyzer/verdict-stage.test.ts`; `npm -w apps/web run build`.
**Learnings:** Not appended to `Role_Learnings.md`; lesson is local to this fix and captured in Warnings.
