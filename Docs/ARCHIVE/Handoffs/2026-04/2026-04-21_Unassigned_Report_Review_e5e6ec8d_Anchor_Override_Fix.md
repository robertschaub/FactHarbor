---
### 2026-04-21 | Unassigned | Codex (GPT-5) | Report Review e5e6ec8d Anchor Override Fix
**Task:** Investigate hosted job `e5e6ec8da824491c8984a18505481ba7` (`UNVERIFIED / Insufficient evidence`) against local comparator `ae6b54e96bda4a54a685731fddc099bc`, find the root cause, and fix it.
**Files touched:** `apps/web/src/lib/analyzer/claim-extraction-stage.ts`; `apps/web/test/unit/lib/analyzer/claim-contract-anchor-override.test.ts`
**Key decisions:** Confirmed the user-linked comparator was the same PDF URL and same prompt hash/commit, not a Bolsonaro run. The hosted job was hidden but inspectable with the local admin key from `apps/web/.env.local`. Root cause was Stage 1 final contract revalidation, not retrieval variance: the hosted run aborted before research because `evaluateClaimContractValidation(...)` treated an uncited article-level limitation anchor (`"The results are limited in their significance"`) as a hard anchor-provenance failure and flipped `preservesContract` to false, even though the validator summary said the extracted claim set still preserved the article contract. The fix narrows the structural override so it only fires when the validator actually cites anchor carrier IDs and none survive structural validation, or when those cited carriers contradict the per-claim assessments. An empty `preservedInClaimIds` array now defers to the validator’s own top-level assessment instead of forcing `report_damaged`.
**Open items:** No fresh live rerun was submitted. The hosted job should be rerun on the current stack to confirm it now proceeds into Stage 2 instead of terminating after Stage 1.
**Warnings:** The remote report required the admin key from `apps/web/.env.local` to inspect because the report is hidden. I did not change prompts. I did not run any expensive live-analysis suites.
**For next agent:** The concrete regression anchors are the hosted hidden job `e5e6ec8da824491c8984a18505481ba7` (`2026-04-21T10:42:34Z`, commit `ace3c114463ecfbe56f4f16037bc5c3a65088e50`, prompt hash `5a77affe3d1b03104ce3ab850a3cf86b6953a890134f1d9dbfc73791d75fcbd7`) and the successful local comparator `ae6b54e96bda4a54a685731fddc099bc` (`2026-04-21T05:08:52Z`, same prompt hash and commit family). The key runtime seam is [claim-extraction-stage.ts](/c:/DEV/FactHarbor/apps/web/src/lib/analyzer/claim-extraction-stage.ts:2694), especially the `noValidIds` override logic. Verification passed on `test/unit/lib/analyzer/claim-contract-anchor-override.test.ts`, `test/unit/lib/analyzer/contract-revalidation-fallback.test.ts`, `test/unit/lib/analyzer/claim-contract-validation.test.ts`, and `npx tsc -p apps/web/tsconfig.json --noEmit`.
**Learnings:** no
