---
### 2026-03-22 | Senior Developer | Codex (GPT-5) | ClaimBoundary Config Provenance Repair
**Task:** Restore reliable per-job config provenance for normal ClaimBoundary jobs so future localhost and quality experiments can be traced without git archaeology.
**Files touched:** `apps/web/src/lib/analyzer/types.ts`, `apps/web/src/lib/analyzer/claimboundary-pipeline.ts`, `apps/web/test/unit/lib/analyzer/claimboundary-pipeline.test.ts`
**Key decisions:** Kept the fix deliberately low-risk. Added optional `jobId` to `CBResearchState`, threaded `jobId` through the existing ClaimBoundary config loads, recorded prompt provenance once at startup via `loadPromptConfig("claimboundary", jobId)`, recorded SR usage via `getConfig("sr", "default", { jobId })`, and captured a full job config snapshot once at startup with `captureConfigSnapshotAsync(...)`. Avoided a broad refactor of prompt-loader or `loadAndRenderSection()`. Verified with a fresh restarted localhost job `68c9e85ad5fc44a58e0f7749312a5872`: `/api/fh/jobs/[id]/configs` returned five config types (`pipeline`, `search`, `calculation`, `prompt`, `sr`) and `/api/admin/quality/job/[id]/config` returned a full snapshot. Targeted test file stayed green (`291/291`) and web build passed.
**Open items:** The live verification job was still running when provenance was checked; this is fine because config usage and snapshot are created during startup/run, not only on completion. No API/UI changes were needed because the existing endpoints started returning meaningful data again once ClaimBoundary began recording usage. If later needed, prompt provenance could be made even more granular by threading `jobId` through `loadAndRenderSection()`, but that was intentionally deferred.
**Warnings:** The verification job’s `gitCommitHash` still showed the last committed hash (`d163aa8c...`) because the fix was verified before commit. For this check, proof came from the restored config rows and snapshot, not from the job hash. Also note that the new unit test emits an existing metrics 404 stderr line because it uses a fake `jobId`; the test still passes and no runtime behavior is affected.
**For next agent:** Config provenance is no longer the blocker for future localhost analysis. Use job `68c9e85ad5fc44a58e0f7749312a5872` as the reference example: it proves the repaired flow on a fresh restarted stack. The next planned execution item from the Mar-22 plan is WS-1 dead-code cleanup, not more provenance work.
**Learnings:** no
