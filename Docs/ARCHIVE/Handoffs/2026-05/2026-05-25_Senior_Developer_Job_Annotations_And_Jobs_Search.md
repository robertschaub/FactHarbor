---
### 2026-05-25 | Senior Developer | Codex (GPT-5) | Job Annotations And Jobs Search
**Task:** Add admin job annotations visible in `/jobs`, and let `/jobs` search match job ID and commit ID prefixes.
**Files touched:** `apps/api/Data/Entities.cs`; `apps/api/Data/FhDbContext.cs`; `apps/api/Program.cs`; `apps/api/Services/JobService.cs`; `apps/api/Controllers/JobsController.cs`; `apps/api/migrations/005_add_admin_annotation.sql`; `apps/web/src/app/api/fh/jobs/[id]/annotation/route.ts`; `apps/web/src/app/jobs/page.tsx`; `apps/web/src/app/jobs/page.module.css`; `apps/web/src/app/jobs/[id]/page.tsx`; `apps/web/src/app/jobs/[id]/page.module.css`; `Docs/AGENTS/Handoffs/2026-05-25_Senior_Developer_Job_Annotations_And_Jobs_Search.md`; `Docs/AGENTS/Agent_Outputs.md`; `Docs/AGENTS/index/handoff-index.json`; `Docs/AGENTS/index/stage-map.json`; `Docs/AGENTS/index/stage-manifest.json`
**Key decisions:** Reused the existing job service/controller patterns instead of adding a parallel admin store. `AdminAnnotation` is nullable, admin-only in list/detail responses, and PATCHed through an admin-protected Next proxy. `/jobs?q=...` now keeps input text search and also performs structural job ID prefix matching for all visible jobs, plus admin-only commit prefix matching. Startup now ensures the new SQLite column exists for local DBs, and a manual SQL migration is present for explicit DB updates.
**Open items:** No implementation blocker. Local DB rows currently had no `gitCommitHash`, so commit-prefix search could not be live-exercised against real local data; the path builds and shares the existing commit-hash fields/filter semantics.
**Warnings:** The first browser check hit a stale API process and job-ID search returned zero results; classified as `keep` because builds passed and the old process lacked the new backend search code. After refreshing local services, `/jobs` job-ID prefix search returned the expected single job. Annotation save was not live-tested with a real job to avoid mutating existing job records; unauthenticated PATCH correctly returned 401.
**For next agent:** If validating manually, restart API/Web before testing because backend search and the annotation endpoint require the updated API process. Use `/jobs`, search for an existing job prefix such as `e2fee7bc`, and use an admin session to open the card action menu or job detail annotation button. The new endpoint is `PATCH /api/fh/jobs/[id]/annotation` with body `{ "annotation": "..." }`.
**Learnings:** No new role learning appended.
