---
### 2026-05-29 | Lead Architect + LLM Expert | Codex (GPT-5) | Stage 1 Contract Carrier Preservation Root Fix
**Task:** Implement and review a root fix for the Bolsonaro-style Stage 1 regression where centrality/Gate 1 could drop a thesis-direct branch from a contract-approved multi-assertion claim set.
**Files touched:** `apps/web/src/lib/analyzer/claim-extraction-stage.ts`; `apps/web/test/unit/lib/analyzer/claim-contract-validation.test.ts`; `apps/web/test/unit/lib/analyzer/claimboundary-pipeline.test.ts`.
**Key decisions:** Completed the existing contract-carrier mechanism instead of adding another repair layer. `evaluateClaimContractValidation` now receives `inputClassification` and, for clean `multi_assertion_input`/`article` outputs, derives `contractCarrierClaimIds` from validator-approved thesis-direct claims (`preservesEvaluativeMeaning=true`, `recommendedAction=keep`, `proxyDriftSeverity=none`) plus anchor carriers. Single-claim outputs remain centrality-disciplined. Two independent reviewers approved the root-cause and LLM/prompt-contract compliance shape.
**Open items:** Commit/restart before live validation. Run the Captain-approved Bolsonaro variants and inspect claim count, carrier survival, and any remaining `report_damaged` or verdict-integrity failures.
**Warnings:** This is mocked and build-verified but not yet live-job verified. Residual risk is over-protection if the contract validator marks too many thesis-direct claims as clean; current mitigation is the strict clean-assessment gate. No prompt/config changes were made and Anthropic prompt caching remains off.
**For next agent:** Review `contractCarrierClaimIds` flow from `evaluateClaimContractValidation` through `getProtectedContractCarrierIds`, `selectClaimsForGate1`, and Gate 1 restoration. Validation already passed: focused carrier/selection suites, full safe `npm test`, web build, and `git diff --check`.
**Learnings:** Not appended to `Role_Learnings.md`; no new durable role-level learning beyond the existing root-cause/failed-attempt recovery guidance.
