---
### 2026-07-01 | Agents Supervisor | Codex (GPT-5) | Codex Model Routing Investigation + Agent Setup
**Task:** Investigate whether the July 1 Claude Code just-in-time model and effort routing should be mirrored for Codex GPT, then implement the low-risk recommendation after Captain approval.
**Files touched:** `.codex/agents/scout.toml`, `.codex/agents/routine-dev.toml`, `.codex/agents/long-haul.toml`, `.codex/agents/verify.toml`, `Docs/AGENTS/Handoffs/2026-07-01_Agents_Supervisor_Codex_Model_Routing_Investigation.md`, `Docs/AGENTS/Agent_Outputs.md`, `Docs/AGENTS/index/handoff-index.json`
**Key decisions:** Do not mirror the Claude mechanism exactly. The Claude commit `4a2a83679` works by moving effort off an env-var floor, adding an advisory `UserPromptSubmit` hook, and creating model/effort-pinned `.claude/agents/` sidecars. The hook only nudges; it cannot switch model or effort. Codex supports the useful part through project custom agents in `.codex/agents/*.toml`, with optional `model` and `model_reasoning_effort`, and can choose a balanced setup when model names are left unpinned. Implemented standalone Codex custom agents only: `scout` (`low`, read-only), `routine-dev` (`medium`), `long-haul` (`high`), and `verify` (`high`, read-only). Skipped `.codex/config.toml` registration because the GPT reviewer noted standalone project agents are the primary documented surface and registration is optional.
**Open items:** Validate in a fresh Codex session that the project agents are discovered in this installed Codex version. Add `.codex/config.toml` only if registration or global `[agents]` limits prove necessary. If future evidence shows a role needs a model pin, validate current model slugs and account availability before adding `model`.
**Warnings:** Avoid porting the Claude keyword/regex advisory hook as authoritative Codex routing. It is English-cue-based, non-semantic, requires hook trust, and is unnecessary unless repeated manual-routing failures appear. Read-only agent files set `sandbox_mode = "read-only"`, but Codex may reapply live parent runtime overrides; do not rely on instructions alone for safety. Do not touch the application pipeline model routing (`apps/web/src/lib/analyzer/model-tiering.ts`) for this harness question.
**For next agent:** If these agents need adjustment, keep them standalone unless there is a concrete need for `.codex/config.toml`. Keep model names unset by default; tune `model_reasoning_effort` first. `verify` should remain adversarial and non-mutating; if tests cannot run in read-only mode, report the command as a verification gap rather than weakening the role.
**Learnings:** Not appended to `Role_Learnings.md`; this is a task-specific Codex surface finding, not a general role lesson yet.

Sources checked: current repo commit `4a2a83679`; `.claude/settings.json`; `.claude/agents/{scout,routine-dev,long-haul,verify}.md`; `scripts/hooks/task-tier-advisor.cjs`; OpenAI Codex manual fetched 2026-07-01 (`/codex/config-basic`, `/codex/config-advanced`, `/codex/models`, `/codex/subagents`, `/codex/hooks`); GPT reviewer verdict `HOLDS WITH CAVEATS`.
