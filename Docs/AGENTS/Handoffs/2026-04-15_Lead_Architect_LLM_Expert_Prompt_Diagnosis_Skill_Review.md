---
### 2026-04-15 | Lead Architect + LLM Expert | Codex (GPT-5) | Prompt Diagnosis Skill Review
**Task:** Review `.claude/skills/prompt-diagnosis/SKILL.md` as a Senior Architect / LLM Expert, execute key parts where useful, and propose improvements if meaningful.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-15_Lead_Architect_LLM_Expert_Prompt_Diagnosis_Skill_Review.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** The skill is conceptually useful but currently overclaims prompt exactness, is not directly runnable in this Windows/PowerShell workspace, and has several diagnosis heuristics that will create false negatives or noisy register entries. The strongest issues are: relying on `executedWebGitCommitHash` instead of runtime prompt provenance (`promptContentHash` / UCM blobs), using Bash-specific commands, using `confidence < 0.4` against reports that store confidence on a 0..100 scale, and diagnosing whole prompt files instead of loaded prompt sections. Methodologically, `P5` is benchmark-specific, `P6` is framed too narrowly as "missing chain-of-thought", and Phase 6 would append speculative findings into the register too aggressively.
**Open items:** If the Captain wants this improved, the next step should be a targeted rewrite of `SKILL.md` plus its companion docs section in `Docs/DEVELOPMENT/Claude_Code_Skills.md` so the documented behavior matches the implemented workflow. A good revision would also switch discovery to structured report fields (`analysisWarnings`, `analysisIssueMessage`, `promptContentHash`) before falling back to logs.
**Warnings:** The current skill text says it correlates failures with the "exact prompts and code active at generation time", but runtime prompt loading is DB-first / UCM-managed and the repo already treats `executedWebGitCommitHash` as whole-repo provenance rather than analyzer-exact provenance. Treat any finding produced by the current version as approximate unless the active prompt blob is confirmed.
**For next agent:** Primary anchors for this review are `.claude/skills/prompt-diagnosis/SKILL.md`, `Docs/ARCHITECTURE/Prompt_Architecture.md`, `apps/web/src/lib/config-loader.ts`, `apps/web/src/lib/analyzer/claimboundary-pipeline.ts`, `apps/web/src/lib/internal-runner-queue.ts`, and `Docs/AGENTS/Handoffs/2026-04-04_senior_developer_investigate_executed_web_commit_hash_confus.md`. Live evidence gathered during review: `ls -t test-output/` fails in PowerShell, recent `test-output/` contents include a top-level JSON file as well as directories, and `phase7-e2` reports use confidence values like `0`, `23`, `58`, `88`, not `0.0..1.0`.
**Learnings:** no
