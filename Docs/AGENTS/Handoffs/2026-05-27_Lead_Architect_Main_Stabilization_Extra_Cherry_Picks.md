---
### 2026-05-27 | Lead Architect | Codex (GPT-5) | Main Stabilization Extra Cherry Picks
**Task:** Continue stabilized-main recovery by taking selected low-risk commits after `406393c9`, validating locally, and pushing if grounded.
**Files touched:** `apps/web/src/lib/analyzer/claim-selection-recommendation.ts`, `apps/web/src/lib/analyzer/aggregation-stage.ts`, `apps/web/src/lib/analyzer/verdict-stage.ts`, `apps/web/src/lib/config-schemas.ts`, `apps/web/configs/calculation.default.json`, `apps/web/src/app/admin/config/page.tsx`, related unit tests.
**Key decisions:** Pushed the analysis-code layer `3e94e53e661320c28f0eb1808031103d811ff710` to `origin/main`, adding cherry-picks for longer claim-selection reason display, zero-citation INSUFFICIENT exclusion from article aggregation, bounded narrative-confidence downgrade, safeDowngrade telemetry split, Option E grounding-policy leak fix, and admin exposure of `narrativeConfidenceMaxDownwardDelta`. During `a131bb41` conflict resolution, preserved the current stabilized direction-repair behavior and only took the new telemetry split, avoiding behavior from skipped integrity-cascade commits.
**Open items:** F1 Stage 1 contract-retry non-convergence remains unresolved. In validation, first hydrogen run `a8fc5d9eb91e479eac20070450470eb0` hit report-level `UNVERIFIED`/`report_damaged`; immediate reruns `e6bb6cf61c0b464e926a959cb95a38d0` and `82028fee20104e02a51cba35c5d6afae` passed as `FALSE`, so this was treated as known stochastic F1 variance, not a regression from the extra cherry-pick layer.
**Warnings:** Local worktree had an uncommitted one-line `APICallError` mock change in `apps/web/test/unit/lib/analyzer/claimboundary-pipeline.test.ts` after the push; it was not created or committed by this stabilization pass. Validation was not pristine because of the single hydrogen F1 failure.
**For next agent:** The pushed analysis-code layer is `3e94e53e`; a documentation-only handoff commit may sit above it on `main`. Safety branch before extra picks is `codex/stabilized-main-before-extra-picks-2026-05-27` at `406393c9`. Verification before push: focused selector/verdict/config tests passed (263 tests), focused aggregation/narrative-cap tests passed (5 tests), and `npm -w apps/web run build` passed. Live jobs on `3e94e53e+e3b0c442`: plastic `ebc9c696ff6d48488f215abee445c52f` passed report-level as `MOSTLY-FALSE` with one claim-level insufficiency; Bolsonaro `076f8a43107b48c1919a9af69e239027` passed report-level as `MIXED` with one claim-level UNVERIFIED; hydrogen first failed F1, then two reruns passed.
**Learnings:** No new role learning appended.
