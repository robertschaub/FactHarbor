---
### 2026-04-16 | LLM Expert | GitHub Copilot (GPT-5.4) | Refinement Gate Restoration And Test Realignment
**Task:** Prepare the surgical patch that restores the immediate pre-`308d00cf` primary-source refinement gate semantics while retaining the separate Stage 1 contract-repair prompt hardening already on `main`.
**Files touched:** `apps/web/src/lib/analyzer/research-orchestrator.ts`; `apps/web/test/unit/lib/analyzer/primary-source-refinement.test.ts`.
**Key decisions:** Restored `claimNeedsPrimarySourceRefinement(...)` to the pre-`308d00cf` contract by requiring `expectedMetrics.length > 0` and a non-empty primary target type set before refinement can trigger. Removed `hasInstitutionalNumericCoverageFallback(...)` entirely because it only existed to mitigate the later relaxed gate. Replaced the old source-type-only refinement test with the inverse regression test that now asserts refinement does **not** fire when `expectedMetrics` is empty, even if `expectedSourceTypes` includes a primary type. The earlier Stage 1 prompt hardening in `CLAIM_CONTRACT_REPAIR` was intentionally left in place and was re-verified via its focused test.
**Open items:** Live rerun the three previously `UNVERIFIED` local jobs (`41a50b26...`, `ea6e7933...`, `2f8cab55...`) on the current tree to confirm the Stage 1 and Stage 2 paths now behave as intended.
**Warnings:** This change deliberately supersedes the earlier asylum-specific fallback documented in the prior handoff. The previous handoff that described `hasInstitutionalNumericCoverageFallback(...)` is now historical context only, not the current implementation.
**For next agent:** Verification completed locally with `npm -w apps/web test -- test/unit/lib/analyzer/primary-source-refinement.test.ts test/unit/lib/analyzer/claim-extraction-prompt-contract.test.ts` and `npm -w apps/web exec tsc --noEmit`. Both succeeded. No references to `hasInstitutionalNumericCoverageFallback` remain.
**Learnings:** no — this was a narrow semantic restoration and test realignment, not a new reusable repository pattern.
