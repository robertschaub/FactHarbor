---
### 2026-05-15 | Senior Developer | Codex (GPT-5) | V2 Pre-Slice7 Context And Diagnostics Hardening
**Task:** Address Senior Architect review findings A1/A2 before Slice 7 while preserving the V2 pre-cutover gate.
**Files touched:** `apps/web/src/lib/analyzer-v2/run-context.ts`; `apps/web/src/lib/analyzer-v2/claim-understanding/runtime-stage.ts`; `apps/web/src/lib/analyzer-v2/claim-understanding/runtime-dispatch.ts`; `apps/web/src/lib/analyzer-v2/claim-understanding/model-adapter.ts`; `apps/web/src/lib/analyzer-v2/orchestrator.ts`; `apps/web/src/lib/analyzer-v2/result-envelope.ts`; focused Analyzer V2 tests; this handoff.
**Key decisions:** Implemented A1 as an inert `PipelineRunContext` snapshot: copied gateway/model policy metadata, prompt-profile references, empty pre-provider-wiring config snapshot, and a pre-cutover observability ledger handle are created once with the run context and passed through the runtime path. Removed live model-policy registry lookup from the Claim Understanding adapter; runtime dispatch now receives gateway/model policy from the run context path. Implemented A2 only as redacted admin-only preparation diagnostics: the damaged public envelope still does not expose `ClaimContract`, raw runtime state, provider telemetry, side effects, prompt provenance, cache decisions, or exact dispatch internals.
**Review/debate:** Guardrail Challenger, Architecture Advocate, and Reconciler all returned `MODIFY`: A1 is safe as inert references; A2 must not expose raw runtime state. The final patch follows the redacted-diagnostic shape and keeps public model/config provenance hashes null until a later result-provenance gate.
**Verification:** Focused verifier passed 6 files / 65 tests. Full Analyzer V2 plus analyzer-v2-runtime verifier passed 20 files / 166 tests. `npm -w apps/web run build` passed with postbuild reseed reporting 0 config changes, 0 prompt changes, 0 errors. `git diff --check` passed.
**Open items:** A3 remains a future UCM activation-seam design gate before provider source wiring. A4/A5/A8 remain future typed-adapter/ACS-dispatch/side-effect-shape cleanup items. No live jobs were run because this is structural and pre-cutover.
**Warnings:** Do not treat the run-context static policy snapshot as runtime approval. It is provenance/input to stages only. Prompt/model/cache approvals, executable gateway status, provider factory wiring, file seeding, cache IO, public API/UI/report exposure, ACS/direct URL execution, and live jobs remain blocked without separate approval.
**DEBT-GUARD RESULT:** Classification: incomplete-existing-mechanism plus planned pre-cutover debt. Chosen option: amend existing run context/orchestrator/adapter path. Rejected path: public partial Claim Understanding exposure or raw runtime-state threading, because that violates the current guardrails. What was removed/simplified: adapter singleton model-policy lookup and `void claimUnderstandingState`. What was added: run-context policy snapshot fields and redacted admin-only diagnostics. Net mechanism count: execution unchanged; provenance and diagnostics made explicit. Debt accepted and removal trigger: static pre-cutover config snapshot remains until the approved UCM activation/provider-wiring gate supplies real config snapshot ownership.
