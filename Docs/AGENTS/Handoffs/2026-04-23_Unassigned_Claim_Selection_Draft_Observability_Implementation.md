---
### 2026-04-23 | Unassigned | Codex (GPT-5) | Claim Selection Draft Observability Implementation
**Task:** Implement the reviewed ACS slow-path observability slice after checkpoint commit `8c25ca79`, keeping `ClaimSelectionDraftState.observability` authoritative and row-level milestone text convenience-only.
**Files touched:** `apps/web/src/lib/analyzer/types.ts`; `apps/web/src/lib/analyzer/claim-extraction-stage.ts`; `apps/web/src/lib/internal-runner-queue.ts`; `apps/web/src/app/analyze/select/[draftId]/page.tsx`; `apps/api/Data/Entities.cs`; `apps/api/Services/ClaimSelectionDraftService.cs`; `apps/api/Controllers/ClaimSelectionDraftsController.cs`; `apps/api/Program.cs`; `apps/api/migrations/005_add_claim_selection_draft_last_event_message.sql`
**What changed:** Added canonical draft-preparation telemetry to `ClaimSelectionDraftState.observability` with phase identity, branch, separated `stage1Ms` / `recommendationMs` / `totalPrepMs`, and Stage 1 sub-step timings. Added transient `CBResearchState.stage1Observability` so Stage 1 can surface contract-validation / retry / repair timings without polluting `PreparedStage1Snapshot`. Split the Stage 1 progress plateau into truthful milestones: contract validation `24`, retry Pass 2 `25`, retry validation `26`, repair `27`, repair validation `28`, Gate 1 `29`, final contract refresh `30/31`. The draft runner now persists observability on prepared and failed drafts, including `auto_continue`, `stage1_failed`, `recommendation_failed`, and `no_candidate_claims` branches. Added `LastEventMessage` to `ClaimSelectionDrafts`, persisted it on live status updates, exposed it via the public draft GET, and added a manual SQL migration plus startup upgrade logic. The draft page now shows the current live milestone during preparation and a preparation timing summary once the draft is prepared or failed.
**Validation:** `npm -w apps/web run build` passed. `npm -w apps/web exec vitest run test/unit/lib/internal-runner-queue.test.ts` passed. `npm -w apps/web exec vitest run test/unit/lib/drain-runner-pause.integration.test.ts -t "drainRunnerQueue returns early when system is paused (network) and probe fails"` passed. `dotnet build apps/api/FactHarbor.Api.csproj -p:UseAppHost=false` passed, with a warning that the running `FactHarbor.Api.exe` remained locked. Full `npm test` did not finish cleanly: one pause-integration timeout and two `test/unit/app/api/admin/test-config/route.test.ts` failures still occurred in the full-suite run.
**Warnings:** I did not restart or stop the running API process, so the standard apphost build still failed on the executable lock and I did not run a live browser/API smoke after the patch. The new DB field depends on either the startup column-upgrade path in `Program.cs` or manual application of `apps/api/migrations/005_add_claim_selection_draft_last_event_message.sql`.
**For next agent:** Restart the web/API services, create a fresh 5+ claim draft on the known slow Iran URL, and verify that the draft page now shows live milestone text through the Stage 1 retry path and stores a timing summary in `draftStateJson.observability`. If full-suite stability matters, investigate the two `admin/test-config` failures separately from this ACS slice and decide whether the one full-suite pause-integration timeout is a flake or needs a test-harness fix.
**Learnings:** The real draft observability hole was not the runner callback but the API service discarding `eventMessage`; once `ClaimSelectionDraftService.UpdateStatusAsync(...)` persisted that text, the UI could show live milestones even for progress values normalized away from the bar. `prepareStage1Snapshot(...)` already returning `state` made it possible to carry Stage 1 timing data out of `extractClaims(...)` without changing the prepared Stage 1 contract.
