### 2026-04-18 | Senior Developer | Codex (GPT-5) | Asylum WWII Stage4 Comparator Reconstruction Prompt Fix
**Task:** Address the Lead Developer review by continuing on the isolated Stage 4 historical-comparison gap, starting with a prompt-only fix rather than more retrieval work.
**Files touched:** `apps/web/prompts/claimboundary.prompt.md`; `apps/web/test/unit/lib/analyzer/verdict-prompt-contract.test.ts`; `Docs/AGENTS/Handoffs/2026-04-18_Senior_Developer_Asylum_WWII_Stage4_Comparator_Reconstruction_Prompt_Fix.md`; `Docs/AGENTS/Agent_Outputs.md`; `Docs/AGENTS/Role_Learnings.md`
**Key decisions:** I inspected stored live job `23d05e2f16d9493d9a2a37a215d9813c` before editing. The residual regression is not that Stage 1 failed to preserve the comparison: `AC_02` already states the current-vs-WWII comparison directly, while `CV_AC_02` is downgraded by Stage 4 reasoning that hand-builds a lower historical comparator from selected categories and assumed non-overlap/duration. I therefore left claim extraction unchanged and tightened only `VERDICT_ADVOCATE` and `VERDICT_RECONCILIATION` so unsupported reconstructed comparators remain confidence/misleadingness caveats instead of decisive contradiction. The new rules also block silently upgrading a colloquial magnitude claim into a stricter synchronized-stock / endpoint-only / same-method test unless the AtomicClaim explicitly states that requirement.
**Open items:** No live rerun was executed in this pass. The next validation step is a fresh run of the approved asylum/WWII input on the newly reseeded prompt hash (`d45a83a732d0...` from the build reseed log) and inspection of whether `CV_AC_02` still relies on unsupported historical endpoint reconstruction. If the article still underperforms after this Stage 4 fix, then revisit the review’s secondary question about merging the standalone current-total claim with the comparison claim in Stage 1.
**Warnings:** The worktree already contained uncommitted retrieval/acquisition/prompt changes from the prior task; I edited only the Stage 4 prompt rules and their contract test on top of that state. The AEM-selector reservation from the review remains open as a future simplification concern if a second CMS-specific discovery path appears; this pass did not touch retrieval. `npm -w apps/web run build` triggers `postbuild` prompt reseeding and refreshed the active `claimboundary` prompt in `apps/web/config.db`; treat that reseed as intentional.
**For next agent:** Start with stored job `23d05e2f16d9493d9a2a37a215d9813c`. Its `claimVerdicts` show the exact failure mode: `CV_AC_01` is already `MOSTLY-TRUE`, while `CV_AC_02` is dragged down by a hand-built lower historical comparator from selected subgroups and assumed simultaneity. The new prompt text is in [claimboundary.prompt.md](/c:/DEV/FactHarbor/apps/web/prompts/claimboundary.prompt.md) around `VERDICT_ADVOCATE` lines 1199-1204 and `VERDICT_RECONCILIATION` lines 1390-1394, with lock-in checks in [verdict-prompt-contract.test.ts](/c:/DEV/FactHarbor/apps/web/test/unit/lib/analyzer/verdict-prompt-contract.test.ts). If you rerun, compare the new `CV_AC_02.reasoning` against the old pattern “theoretical maximum / not all simultaneously present”; that phrasing should now be demoted to caveat territory rather than decisive contradiction.
**Learnings:** yes — appended one Senior Developer learning about blocking unsupported comparator arithmetic in Stage 4 prompts instead of letting it silently become a truth downgrade.
