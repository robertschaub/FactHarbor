---
### 2026-04-20 | Unassigned | Codex (GPT-5) | Bundesrat Stage 1 Prompt Review
**Task:** Review the current Bundesrat Stage 1 issue as a prompt-design problem first, focusing on salience, Pass 2 extraction, contract validation, and single-claim atomicity.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-20_Unassigned_Bundesrat_Stage1_Prompt_Review.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** The safest prompt-only improvement is a single generic `priority-anchor` invariant, not a new decomposition rule. Make salience emit anchors in preservation-priority order, with any truth-condition-bearing modifier on the thesis-defining main act ranked ahead of branch-only temporal/conditional spans. Then mirror that invariant in Pass 2 / contract validation / single-claim atomicity so coordinated-branch splitting changes only the varying branch event while the same modifier-bearing main-act wording is repeated in every in-scope branch claim. This preserves the intended two-claim decomposition without keyword logic or schema changes.
**Open items:** Prompt-only tightening should be treated as additive. It may improve first-pass stability, but it should not replace the existing runtime contract-repair and single-claim atomicity enforcement path.
**Warnings:** Current prompt text already contains modifier-fusion, bundling-prohibition, and atomicity-audit language. The fact that the Bundesrat family still historically produced one bundled claim means prompt doctrine alone is not fully sufficient; runtime enforcement remains part of the safe fix. Any prompt wording that overstates the priority anchor could accidentally push the model back toward one near-verbatim whole claim unless it explicitly says the priority anchor controls repeated wording, not claim count.
**For next agent:** Relevant prompt anchors are `apps/web/prompts/claimboundary.prompt.md` under `CLAIM_SALIENCE_COMMITMENT`, `CLAIM_EXTRACTION_PASS2`, `CLAIM_CONTRACT_VALIDATION`, and `CLAIM_SINGLE_CLAIM_ATOMICITY_VALIDATION`. Relevant runtime context is `apps/web/src/lib/analyzer/claim-extraction-stage.ts`, especially `shouldRunSingleClaimAtomicityValidation(...)`, `evaluateSingleClaimAtomicityValidation(...)`, `applySingleClaimAtomicityValidation(...)`, and `selectRepairAnchorText(...)`. The review conclusion is: prompt-only change is useful, but existing runtime stabilization must remain.
**Learnings:** no
