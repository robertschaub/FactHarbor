---
### 2026-04-29 | Senior Developer | Codex (GPT-5) | Job Admin Annotations
**Task:** Support administrators annotating a job, with annotations visible only to administrators in the jobs list and report header.
**Files touched:** `apps/api/Controllers/JobsController.cs`; `apps/api/Data/Entities.cs`; `apps/api/Data/FhDbContext.cs`; `apps/api/Program.cs`; `apps/api/Services/JobService.cs`; `apps/api/migrations/008_add_job_admin_annotation.sql`; `apps/web/src/app/api/fh/jobs/[id]/annotation/route.ts`; `apps/web/src/app/jobs/page.tsx`; `apps/web/src/app/jobs/page.module.css`; `apps/web/src/app/jobs/[id]/page.tsx`; `apps/web/src/app/jobs/[id]/page.module.css`; `apps/web/test/unit/app/api/fh/jobs-routes.test.ts`.
**Key decisions:** Stored annotations as nullable `Jobs.AdminAnnotation` metadata, updated only through `JobService`, and returned the field only when `X-Admin-Key` is valid. Added a web proxy route that preserves the existing admin-key boundary, plus admin-only editor modals on the jobs list and report page. Added the visible report-header banner only for authenticated administrators.
**Open items:** None.
**Warnings:** Existing databases get the column through `EnsureJobsColumn(...)` at API startup; manual SQL migration `008_add_job_admin_annotation.sql` is included for environments that apply SQL migrations directly. The job event audit records only that the annotation was updated or cleared, never the annotation text.
**For next agent:** Use `PUT /api/fh/jobs/{jobId}/annotation` from the web app or `PUT /v1/jobs/{jobId}/annotation` on the API with `X-Admin-Key`. Non-admin list/detail responses receive `adminAnnotation: null`, matching the existing admin-only field pattern. Verification passed: `dotnet build apps/api/FactHarbor.Api.csproj`, `npm -w apps/web run build`, `npm test`, and `npm -w apps/web exec vitest run test/unit/app/api/fh/jobs-routes.test.ts`.
**Learnings:** Not appended to `Docs/AGENTS/Role_Learnings.md`; no broadly reusable role gotcha beyond the implementation details above.
