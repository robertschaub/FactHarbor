---
### 2026-04-15 | Lead Developer + LLM Expert | Codex (GPT-5) | Prompt Issue Review Consolidation
**Task:** Review the reported Phase 7 / prompt issues, verify which are real in the current repo, challenge the proposed fixes with parallel agent review, and implement the narrow prompt/frontmatter fixes that are solid.
**Files touched:** `apps/web/prompts/claimboundary.prompt.md`
**Key decisions:** P0 `CLAIM_CONTRACT_REPAIR` was a real bug in `61815f41` but is already fixed in current repo/runtime (`##` header + `${...}` variables + narrow repair schema). Kept that conclusion and did not re-fix it. Implemented the prompt-only slice that is still real: frontmatter drift (`anchorText`, `CLAIM_CONTRACT_REPAIR`), salience taxonomy/rule clarification for finality/binding-effect qualifiers, generic chronology anti-inference counterexample, binding appendix state handling (`success=true/false/empty anchors`), binding tiebreakers, and base validator rule-11 wording so thesis-direct carrier eligibility is explicit and aligned with runtime behavior.
**Open items:** Do not treat the salience model-tier concern as solved. Current salience uses shared `understand` routing; a real fix needs a dedicated salience task/override or UCM-backed routing decision, not a silent switch to `context_refinement`. Prompt-governance gaps outside this slice remain: some analysis-affecting retry/context text is still inline in `claim-extraction-stage.ts`. Two assertions in `apps/web/test/unit/lib/analyzer/claim-contract-validation.test.ts` currently fail on source-string expectations unrelated to this prompt-file change.
**Warnings:** Prompt-specific verification passed (`prompt-frontmatter-drift.test.ts`, `claim-extraction-prompt-contract.test.ts`). The broader `claim-contract-validation.test.ts` suite is not clean, but the failing assertions target pre-existing `claim-extraction-stage.ts` source-shape expectations I did not modify. Runtime prompt loading is DB-backed; local system-seeded prompts auto-refresh from file, but a non-system-edited Admin/UCM prompt would still need reseeding/manual activation to pick up these text changes.
**For next agent:** Treat the accepted slice as prompt-only and already landed in `apps/web/prompts/claimboundary.prompt.md`. If continuing, separate three buckets cleanly: (1) landed prompt fixes, (2) deferred routing/config work for salience tiering, (3) separate prompt-governance cleanup moving inline Stage-1 guidance into UCM-managed sections. Relevant corroborating reviews: `Docs/AGENTS/Handoffs/2026-04-15_LLM_Expert_Phase7_Prompt_Quality_Issue_Investigation.md`, `Docs/AGENTS/Handoffs/2026-04-15_Code_Reviewer_Prompt_Fixes_Skeptical_Review.md`, and `Docs/AGENTS/Handoffs/2026-04-15_LLM_Expert_Prompt_Audit_Validation_Review.md`.
**Learnings:** no — none appended to `Role_Learnings.md`.
