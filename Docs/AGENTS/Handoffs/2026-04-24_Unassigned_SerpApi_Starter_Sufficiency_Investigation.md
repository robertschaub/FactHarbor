### 2026-04-24 | Unassigned | Codex (GPT-5) | SerpApi Starter Sufficiency Investigation
**Task:** Investigate whether SerpApi Starter is sufficient for FactHarbor with one regular user, checking local and deployed installations.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-24_Unassigned_SerpApi_Starter_Sufficiency_Investigation.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Current local and deployed active search provider order is Google CSE first, Serper second, Wikipedia supplementary; SerpApi is configured with credentials locally but disabled in UCM/default search config. If SerpApi is enabled with the same priority as Serper, `auto` provider ordering would place SerpApi before Serper after Google CSE. Local job telemetry over the latest 500 succeeded jobs shows median 15 total search queries/job, p95 24, max 30; actual Serper fallback usage is much lower at median 0, p95 3, max 14 queries/job because Google CSE usually fills results first. SerpApi Starter currently lists 1,000 searches/month and 200/hour at $25/month, so it is sufficient as a Google fallback for one regular user but not sufficient as the primary Google search provider for frequent daily use.
**Open items:** If SerpApi is enabled, set `search.providers.serpapi.enabled=true` in UCM and leave Serper as the next fallback. Re-check usage after several live jobs because SerpApi only counts successful searches and cached/local provider behavior can shift monthly burn.
**Warnings:** Do not assume SerpApi is already active just because `SERPAPI_API_KEY` exists in `.env.local`; UCM currently disables it. Do not replace Google CSE with SerpApi as primary unless accepting roughly 40-70 typical full analyses/month on Starter based on current telemetry.
**For next agent:** Relevant anchors are `apps/web/src/lib/web-search.ts`, `apps/web/configs/search.default.json`, deployed admin `/api/version` showing `["Google-CSE","Serper","Wikipedia"]`, and local `apps/api/factharbor.db` job telemetry from `ResultJson.searchQueries`.
**Learnings:** no
