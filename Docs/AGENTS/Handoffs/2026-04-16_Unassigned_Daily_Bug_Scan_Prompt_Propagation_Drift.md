---
### 2026-04-16 | Unassigned | Codex (GPT-5) | Daily Bug Scan Prompt Propagation Drift
**Task:** Rerun the daily bug scan after the latest prompt-focused commits and verify both safe-suite status and runtime-relevant prompt propagation.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-16_Unassigned_Daily_Bug_Scan_Prompt_Propagation_Drift.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Treated the new review surface as the prompt stack from `f19059c0` through `35a22fba`, then ran the full safe suite plus direct inspection of local UCM prompt state. The safe suite is green on the current tree. The concrete issue is not test failure but prompt propagation drift: the active local `claimboundary` prompt in `apps/web/config.db` is still hash `977aaac7f9cd38bd88cf1c98c6d698328a430ac534025aab278ede07f1989d1a` (`seed-v1.0.1`, activated `2026-04-16T15:43:12.915Z`), while the current prompt file hashes to `db42b6c7fa6ec671cc21c68eb682008f6cdf65316d74e337fda704ce7f377150` after later commits at 17:25–17:51. Local runtime would therefore still use the older prompt unless reseeded or rebuilt.
**Open items:** Run `npm -w apps/web run reseed:prompts` or a full `npm -w apps/web run build` on the intended environment so the active `prompt/claimboundary` row matches the current file. If other environments are admin-managed rather than system-seeded, verify activation there too instead of assuming file state is live.
**Warnings:** The worktree is dirty from prior handoff docs and untracked JSON artifacts (`bolsonaro_baseline.json`, `bolsonaro_result.json`). I did not modify application code or reseed anything during this scan. Also, local Node native `better-sqlite3` is ABI-mismatched in this shell, so I used Python’s read-only `sqlite3` module to inspect `config.db`.
**For next agent:** Reproduce with `npm test` (currently green), then inspect prompt activation via `config_active` + `config_blobs` for `config_type='prompt'` and `profile_key='claimboundary'`. The exact mismatch on this machine is active hash `977aaac7...` vs current file SHA-256 `db42b6c7...`. Relevant files: `apps/web/prompts/claimboundary.prompt.md`, `apps/web/package.json:12-16`, and `apps/web/src/lib/config-storage.ts:1316-1367`.
**Learnings:** no
