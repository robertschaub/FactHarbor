---
### 2026-04-24 | Senior Developer | Codex (GPT-5) | Claim Selection Draft State And Salience Runtime Guards
**Task:** Fix the `/analyze/select/[draftId]` runtime crash when a persisted draft state lacks recommendation fields, then fix the follow-up Stage 1 preparation failure where salience commitment leaked a structured-output/schema error.
**Files touched:** `apps/web/src/app/analyze/select/[draftId]/page.tsx`; `apps/web/src/lib/analyzer/claim-extraction-stage.ts`; `apps/web/test/unit/lib/analyzer/claimboundary-pipeline.test.ts`
**Key decisions:** Kept the UI fix at the page boundary and normalized draft-state array fields from parsed JSON before rendering. Kept the Stage 1 fix at the salience caller boundary: salience commitment is explicitly documented as non-fatal, so leaked schema errors now produce a failed salience status and Stage 1 continues to Pass 2.
**Open items:** No code open items. There is still no dedicated component-level test for the draft page; the web build is the current guard for that client component. The already-failed draft may need a fresh retry after the dev process has picked up the code change.
**Warnings:** The worktree already had unrelated modified files in `Docs/AGENTS/index/` and `Docs/Legal/Vereinsstatuten_FactHarbor_DE_unterschriftsfassung.pdf`; they were not touched for this fix.
**For next agent:** If this page gets more draft-state fields, keep parsed `draftStateJson` treated as untrusted runtime data even when TypeScript marks fields required. The UI crash was caused by `draftState?.recommendedClaimIds.length`, which still dereferenced `.length` when `draftState` existed but `recommendedClaimIds` did not. The preparation failure surfaced at `Extracting claims: salience commitment (audit)` and should remain fail-open unless product explicitly makes salience binding a hard gate.
**Learnings:** Not appended to `Role_Learnings.md`; this is covered by existing structured-output and boundary-hardening patterns.
