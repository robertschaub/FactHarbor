---
### 2026-04-15 | Lead Architect + LLM Expert | Codex (GPT-5) | Prompt Diagnosis Skill Rewrite
**Task:** Rewrite `.claude/skills/prompt-diagnosis/SKILL.md` and its companion documentation so the workflow matches current FactHarbor runtime provenance, PowerShell execution, and report schema.
**Files touched:** `.claude/skills/prompt-diagnosis/SKILL.md`, `Docs/DEVELOPMENT/Claude_Code_Skills.md`, `AGENTS.md`, `Docs/AGENTS/Handoffs/2026-04-15_Lead_Architect_LLM_Expert_Prompt_Diagnosis_Skill_Rewrite.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Re-anchored the skill on `promptContentHash` / `config.db` exact-blob recovery instead of claiming commit-exact prompt reconstruction from `executedWebGitCommitHash`. Rewrote the operational steps to use PowerShell-compatible commands, corrected report discovery to the current 0..100 confidence scale and mixed `test-output/` artifact layout, and updated the deficiency taxonomy so P5 is generic, P6 is "insufficient reasoning scaffold", P7 is not log-only, and P9 reflects rendered-section order rather than whole-file position. Tightened Phase 6 so speculative findings are not auto-appended to the register.
**Open items:** I did not update `GEMINI.md` because its row only points to the skill file and does not repeat the stale provenance claim. If the team wants full doc parity, a small follow-up could update any other narrative references outside `AGENTS.md` and `Docs/DEVELOPMENT/Claude_Code_Skills.md`.
**Warnings:** This was a documentation/skill rewrite only; no analyzer code changed. The new skill text assumes a local Python interpreter is available for the recommended `sqlite3` blob lookup path. If a target environment lacks Python, the skill still has a conceptual fallback path (`COMMIT-APPROXIMATE` / `CURRENT-ONLY`), but the exact blob-recovery step would need an alternate SQLite access method.
**For next agent:** Start with `.claude/skills/prompt-diagnosis/SKILL.md`. The companion summaries in `Docs/DEVELOPMENT/Claude_Code_Skills.md` and `AGENTS.md` are now aligned to the new provenance model. Verification performed: repo-wide grep confirmed the stale "commit-exact" wording is gone from the active skill docs, and the rewritten skill now explicitly references `promptContentHash`, `config.db`, PowerShell commands, the 0..100 confidence scale, and UCM/DB-first prompt runtime.
**Learnings:** no
