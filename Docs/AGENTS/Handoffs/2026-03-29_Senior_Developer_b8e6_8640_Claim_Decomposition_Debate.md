---
### 2026-03-29 | Senior Developer | Codex (GPT-5) | b8e6 + 8640 Claim Decomposition Debate
**Task:** Compare local job `b8e616ef9a5e4678b074f2bb8614b2d1` with deployed job `8640e06255c4455cb97c9c699700b5ed`, determine whether they share a root cause, and propose a consolidated fix strategy.
**Files touched:** `Docs/AGENTS/Handoffs/2026-03-29_Senior_Developer_b8e6_8640_Claim_Decomposition_Debate.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** The two jobs are related but not identical. They do NOT point to a new last-3-days committed regression. `b8e6` is a `single_atomic_claim` over-split into two near-duplicate conjunct claims (`Werkzeuge` vs `Methoden`). `8640` is an `ambiguous_single_claim` explicitly decomposed into three claims, but two of them (`effizient` / `wirksam`) are so close that they both starved evidence and became `UNVERIFIED`. Shared root cause: Stage 1 decomposition control is too permissive, and contract validation is too weak at rejecting non-independent or insufficiently research-separable claim splits. Secondary issue: the fallback dimension-tagging heuristic in `claim-extraction-stage.ts` is too broad because it can tag explicit `single_atomic_claim` outputs as dimension decompositions.
**Open items:** Any prompt-level fix requires explicit human approval because it changes `apps/web/prompts/claimboundary.prompt.md`. The exact dirty-worktree delta for `b8e6` execution build `f1e5cc96...+cbdf73b6` cannot be reconstructed from stored job data alone.
**Warnings:** The fallback dimension-tagging heuristic is not the whole problem. In `b8e6`, the two claims would likely have survived Gate 1 even without the tag because they already passed opinion/fidelity and had specificity scores above the threshold. The primary failure is earlier: Pass 2 + contract validation allowed an over-split set to stand.
**For next agent:** Treat this as a shared Stage-1 claim-decomposition quality problem with two subcases: (1) over-splitting a single disclosure proposition into conjunct-level near-duplicates; (2) over-separating nearby evaluative dimensions that are linguistically distinct but not independently evidence-separable. Smallest robust sequence: first tighten the fallback tag so explicit `single_atomic_claim` is not auto-promoted to dimension decomposition; then strengthen `CLAIM_CONTRACT_VALIDATION` to reject non-independent splits for both `single_atomic_claim` and `ambiguous_single_claim`.
**Learnings:** no
