---
### 2026-05-17 | LLM Expert | Codex (GPT-5) | Pipeline V2 Model Selection Investigation
**Task:** Investigate whether Pipeline V2 should change LLM models for any LLM call, using multi-agent assistance including a Claude review.
**Files touched:** `Docs/AGENTS/Handoffs/2026-05-17_LLM_Expert_V2_Model_Selection_Investigation.md`, `Docs/AGENTS/Agent_Outputs.md`, `Docs/AGENTS/Role_Learnings.md`.
**Key decisions:** Do not change any executable V2 model now. The two hidden executable V2 calls use Anthropic `claude-haiku-4-5-20251001` through static activation snapshots, and the current Query Planning failure is explicitly under X7-U diagnostics before repair selection. Planned downstream tasks have no approved model-policy contracts yet. The evidence-backed next move is model-policy authority plus telemetry, not a model swap.
**Open items:** Run/consume X7-U1 diagnostics before deciding whether the Query Planning repair belongs in prompt text, schema, adapter normalization, provider configuration, or model routing. Build a V2 task-policy/model resolver so `modelTier` and `providerPolicy: "from_config_snapshot"` become load-bearing. Decide one authority for stale `model-tiering.ts`, generated `stage-manifest.json`, and newer `model-resolver.ts` before using legacy tier labels in V2.
**Warnings:** `claim_understanding_gate1` and `evidence_query_planning` both declare `modelTier: "standard"` but execute a hardcoded Haiku model from `run-context.ts`; changing the model today would require approval-package and exact-validator changes, not just config. `evidence_query_planning` is hard-fail exact on model/policy values in both runtime and provider config validation. Official provider catalogs show newer premium options than some V1 helpers, but that is a future verdict/debate policy issue, not justification to change the two hidden V2 calls.
**For next agent:** Start from `apps/web/src/lib/analyzer-v2/gateway/model-policy-registry.ts`, `apps/web/src/lib/analyzer-v2/run-context.ts`, `apps/web/src/lib/analyzer-v2/evidence-lifecycle/query-planning/runtime.ts`, `apps/web/src/lib/analyzer-v2-runtime/evidence-query-planning-provider-runtime-config.contract.ts`, and `Docs/WIP/2026-05-17_V2_Slice_X7-U1_Query_Planning_Diagnostic_Live_Smoke_Package.md`. Claude Opus 4.6 independently agreed: no model changes now; fix authority/measurement first.
**Learnings:** Appended to `Docs/AGENTS/Role_Learnings.md`: for V2 model investigations, distinguish declarative model-tier metadata from the concrete executable activation snapshot and exact validators before recommending model changes.
