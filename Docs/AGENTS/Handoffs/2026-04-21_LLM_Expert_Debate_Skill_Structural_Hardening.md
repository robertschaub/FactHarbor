---
### 2026-04-21 | LLM Expert | Codex (GPT-5) | Debate Skill Structural Hardening
**Task:** Improve `.claude/skills/debate/SKILL.md` as LLM Expert using a structured adversarial review of the skill itself.
**Files touched:** `.claude/skills/debate/SKILL.md`, `Docs/AGENTS/Handoffs/2026-04-21_LLM_Expert_Debate_Skill_Structural_Hardening.md`, `Docs/AGENTS/Agent_Outputs.md`, `Docs/AGENTS/Role_Learnings.md`
**Key decisions:** Kept the core Advocate / Challenger / Reconciler / optional Probe+Validator architecture instead of redesigning the skill. Replaced proposition-keyword auto-routing with a structural routing checklist over a fixed `CONTEXT_MANIFEST` envelope. Added an explicit intake / structural-audit phase that can flag `CONTEXT-THIN`, carry that state into every role brief, and cap confidence when the evidence depth does not justify the tier. Made structural-first behavior real by adding structural sections to Advocate, Challenger, Probe, Reconciler, and Validator contracts. Expanded the final output so `Structural Audit`, `Rejected Arguments`, `Skipped Roles`, and caveats remain visible to the caller.
**Open items:** `Docs/DEVELOPMENT/Claude_Code_Skills.md` still does not publish `/debate` alongside the other shared workflows; if Captain wants cross-tool documentation parity, add a concise `/debate` summary there in a separate docs pass. I did not edit `AGENTS.md` or `Docs/AGENTS/Multi_Agent_Collaboration_Rules.md` because the active worktree already contains unrelated user edits in those files.
**Warnings:** `.claude/skills/debate/` was already untracked in this worktree before the edit, so this task updates the skill in place but does not resolve whether Captain intends to add the directory to git. No automated test suite exists for skill markdown; verification was a full text consistency pass over the rewritten contracts, routing rules, and output schema. The new contract is stricter for callers: any skill that invokes `/debate` programmatically should pass a manifest-like bundle with stable evidence IDs and explicit gaps, not an unstructured narrative blob.
**For next agent:** If another workflow calls `/debate`, update that caller first so it emits `CONTEXT_MANIFEST` with `EVIDENCE_INVENTORY`, `KNOWN_GAPS`, and `OPTIONAL_STATE` only when materially relevant. If Captain wants another bounded improvement, add one or two canonical manifest examples from a real caller skill rather than adding more roles. Primary file: `.claude/skills/debate/SKILL.md`.
**Learnings:** Appended to `Role_Learnings.md`? **Yes** — added a reusable LLM Expert pattern: reusable debate skills should keep the domain schema caller-owned but still force a small structural transport envelope (inventory IDs, gaps, optional provenance/language/runtime state) so every debate role reasons from the same auditable footing.
