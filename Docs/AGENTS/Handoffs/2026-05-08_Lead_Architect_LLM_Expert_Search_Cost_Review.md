---
### 2026-05-08 | Lead Architect / LLM Expert | Codex (GPT-5) | Search API Cost Reduction Review
**Task:** Review the proposed no-code API cost actions: make Serper primary, demote/cap Google CSE, and cancel SerpApi.
**Files touched:** `Docs/AGENTS/Handoffs/2026-05-08_Lead_Architect_LLM_Expert_Search_Cost_Review.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Recommendation is approve with guardrails. Active main search config is `auto` with Google CSE priority 1, Serper priority 2, SerpApi disabled; code sorts auto providers by priority and accumulates results, so the Serper/Google swap is operationally sufficient to route primary search to Serper. Google CSE `dailyQuotaLimit` is not enforced by code, so cost control must happen via Google Cloud/API quota/billing controls, not UCM alone. Official Google docs confirm CSE is $5/1k after 100 free daily queries and is closed to new customers with existing customers transitioning by 2027-01-01, strengthening the demotion case. Serper public pricing currently shows 50k credits for $50 ($1/1k). SerpApi public pricing is $25/month for 1k searches.
**Open items:** The referenced file `Operations/Finance/API Costs/API_Cost_Analysis_2026-05-08.md` was not present in this checkout. Reconcile the claimed ~$155-195/month saving against actual vendor invoices; local DB telemetry proves Google dominates traffic but implies a lower Google-only bill for this local environment. Before disabling Google Cloud billing, verify the CSE key/project is dedicated or use API-specific quota/budget controls; disabling project billing can stop all services in that Google Cloud project. Before canceling SerpApi, disable/remove it from SR evaluation search too, because active `sr.default` UCM has `evaluationSearch.providers.serpapi.enabled=true` even though main pipeline search disables it.
**Warnings:** UCM provider `dailyQuotaLimit` is documented as future-only, not a runtime quota. Leaving Google CSE enabled at priority 2 with billing disabled is acceptable as a free fallback only if 100/day quota exhaustion and circuit-breaker noise are acceptable. Main search SerpApi disabled does not mean every FactHarbor path has SerpApi disabled; SR evaluation owns a separate provider config.
**For next agent:** Relevant anchors: `apps/web/configs/search.default.json`, `apps/web/src/lib/web-search.ts`, `apps/web/src/lib/config-schemas.ts`, `apps/web/configs/sr.default.json`, `apps/web/src/app/api/internal/evaluate-source/route.ts`, `apps/web/config.db`, `apps/api/factharbor.db`, and `Docs/AGENTS/Handoffs/2026-04-24_Unassigned_SerpApi_Starter_Sufficiency_Investigation.md`.
**Learnings:** no
