### 2026-04-18 | Senior Developer | Codex (GPT-5) | Asylum WWII Post-Gating Live Rerun
**Task:** Run the approved asylum/WWII input live after the Concern A relevance-gating fix so the new commit hash is recorded on a real job and confirm whether the SEM 2025 commentary PDF still remains reachable on the golden path.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-18_Senior_Developer_Asylum_WWII_Post_Gating_Live_Rerun.md`; `Docs/AGENTS/Agent_Outputs.md`; `Docs/AGENTS/Role_Learnings.md`
**Key decisions:** Treated the first live submission as invalid because the PowerShell POST path stored `Fl??chtlinge` instead of the Captain-approved `Flüchtlinge`, violating the exact-input rule for approved multilingual inputs. Re-ran via Node with explicit Unicode-safe text. The valid job is `25dea04fb0da4ab5ad12fd5dbf76896a` on commit `7b9dec65d4dcab86a0314fec4ac559f9e5abdae8` and prompt hash `d45a83a732d0883fae10f394c6627b01cca43b7beb0cc44fbd9b4bb2514929e8`.
**Open items:** The exact-input rerun did not reproduce the prior retrieval golden path. It finished `MIXED` / `45` / `65` with one merged claim, 11 queries, 16 sources, and **no** `stat-jahr-2025-kommentar-d.pdf` source in `resultJson.sources[]`. The strongest current hypothesis is that one of two things changed the live path: (1) discovered follow-ups from `news.admin.ch` / `sem.admin.ch` are now being over-rejected by the new relevance gate when only URL-derived title + parent snippet are available, or (2) the one-claim Stage 1/query shape is altering which pages are fetched deeply enough to expose the 2025 SEM artifact. This is an inference from the source/query diff, not directly logged proof.
**Warnings:** Ignore job `d87c15b0ffba42b7a21520fd9cb331e7` for evaluation. It was submitted from PowerShell and stored `Fl??chtlinge`, so it is not a valid Captain-approved-input run even though it targeted the same commit. Also note that the persisted `FetchedSource.relevanceScore` field did not surface as populated in the final report payload on job `25dea...`; all 16 `sources[]` entries were null/absent on that field in the serialized result. That does not invalidate Concern A, but it means the audit/provenance benefit is not yet visible in this report artifact.
**For next agent:** Compare valid rerun `25dea04fb0da4ab5ad12fd5dbf76896a` against pre-gating/golden-path job `23d05e2f16d9493d9a2a37a215d9813c`. The important deltas are: `25dea...` merged to one claim (`AC_01` only), generated 11 queries instead of 13, fetched `https://www.sem.admin.ch/sem/de/home/publiservice/statistik/asylstatistik.html` plus the 2024 commentary PDF, but did not fetch the 2025 SEM commentary PDF or any `cms.news.admin.ch` artifacts. Start with the query/source diff before touching Stage 4 again. If the next task is code, instrument or expose discovery-gate outcomes per job so we can distinguish “not discovered” from “discovered then rejected” without reading transient dev-server logs.
**Learnings:** yes — PowerShell inline POST is unsafe for exact multilingual live submissions on this Windows setup; use Node or another explicit UTF-8-safe path.
