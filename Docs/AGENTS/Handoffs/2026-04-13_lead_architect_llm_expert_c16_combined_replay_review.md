---
### 2026-04-13 | Lead Architect + LLM Expert | Codex (GPT-5) | C16 Combined Replay Review
**Task:** Review `Docs/WIP/2026-04-13_C16_R2_Combined_Replay_Analysis.md` against the cited Phase 5/6 docs, HEAD code, and local `Jobs` data in `apps/api/factharbor.db`.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-13_lead_architect_llm_expert_c16_combined_replay_review.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Treat the review as data-integrity-first. Verified the HEAD code paths cited by the doc, then queried `Jobs.ResultJson` directly from `apps/api/factharbor.db` to check the claimed HEAD cohort and pre-C16 comparator. Main findings: the HEAD cohort includes a non-locked input variant (`99ffd5bb` uses `unterschrieb`, not `unterschreibt`), the pre-C16 comparator misclassifies `0ce78ee9` as a "full pass" even though `VerdictLabel='UNVERIFIED'`, and the "roughly doubled" claim depends on a sampled baseline rather than the full date-window cohort.
**Open items:** Correct the combined replay doc before relying on it for a ship/no-ship decision. Recompute rates using the exact locked R2 input only, and decide whether comparison should be against the full pre-C16 window or an explicitly justified matched cohort.
**Warnings:** Current closure language overstates the measured improvement. With the exact locked input enforced, HEAD is 1/6 full-pass and 2/6 gate-pass in the local DB, not 2/7 and 3/7. The verification snippet in the doc also uses broad substring filtering, so it would silently mix `unterschreibt` and `unterschrieb` variants.
**For next agent:** Start with `Docs/WIP/2026-04-13_C16_R2_Combined_Replay_Analysis.md` lines 11-16, 22-45, and 60-69. Re-query `apps/api/factharbor.db` `Jobs` rows for the locked string `Der Bundesrat unterschreibt den EU-Vertrag rechtskräftig bevor Volk und Parlament darüber entschieden haben`, then update the doc's cohort table and comparison math before any closure/ship recommendation is reused.
**Learnings:** no — review surfaced doc-specific data-integrity issues, but nothing broad enough to append to `Role_Learnings.md`.
