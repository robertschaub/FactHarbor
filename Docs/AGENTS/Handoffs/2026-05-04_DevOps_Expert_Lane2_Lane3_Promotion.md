---
### 2026-05-04 | DevOps Expert | Codex (GPT-5) | Lane 2/3 Promotion To Main
**Task:** Promote the approved Lane 2/3 ACS commits from `codex/integrate-selected-claim-admission-coverage` to `main` together, include the Lane 3 status-doc correction, verify scope, run post-merge verification, restart/reseed, verify active UCM keys, and record residual follow-ups.

**Files touched:** Local `main` was fast-forwarded from `dc402b86` to `78596f5d`, bringing in commits `7c59c8dd`, `4e1bfff2`, `f896c889`, `7417bdfa`, and `78596f5d`. Completion documentation added this handoff and an index row in `Docs/AGENTS/Agent_Outputs.md`. `Docs/AGENTS/index/handoff-index.json` was regenerated by the index workflow after documentation.

**Key decisions:** Promoted the approved commits as one stack, no subset. The only extra promoted source-branch commit is `78596f5d docs(acs): correct lane3 promotion status`, which fixes the stale live-job bullet and records active `config.db` budget-awareness keys in `Docs/WIP/2026-05-03_Selected_Claim_Admission_Coverage_Lane3_Status.md`. Pre-merge scope checks found no app prompt file changes, no legal/private docs, no Stage 4 verdict-generation file changes, and no prepared-routing/applicability paths.

**Verification:** `git diff --check HEAD~5..HEAD` passed before validation. `npm test` failed once on `test/unit/lib/drain-runner-pause.integration.test.ts` with a 5s timeout in the known transient area; failed-attempt classification was `keep` because the isolated file immediately passed and the full safe suite passed on retry. Passed after merge: `npm -w apps/web test -- test/unit/lib/drain-runner-pause.integration.test.ts`, `npm test`, `npm -w apps/web run build`, `dotnet test apps/api.Tests/FactHarbor.Api.Tests.csproj -c Release --no-restore`, and `dotnet build apps/api/FactHarbor.Api.csproj -c Release --no-restore`.

**Runtime:** Services were restarted in hidden background PowerShell processes after config validation, port cleanup, and reseed. Health checks passed for `http://localhost:5000/health` and `http://localhost:3000/api/health`. Reseed reported `Configs: 0 changed, 4 unchanged | Prompts: 0 changed, 3 unchanged | Errors: 0`.

**UCM activation:** Active local `apps/web/config.db` `pipeline/default` hash is `3e7e05e3f20b6829cac11b6ccca0b7c345f0ea09487cf9d5964b01acae3b8075`. Active values are `claimSelectionBudgetAwarenessEnabled = true`, `claimSelectionBudgetFitMode = "allow_fewer_recommendations"`, `claimSelectionEstimatedMainResearchMsPerClaim = 160000`, `claimSelectionMinRecommendedClaims = 1`, and `contradictionProtectedTimeMs = 120000`.

**Open items:** Direct `/v1/analyze` remains a documented ACS bypass for direct API submissions. Budget-aware admission cap defaults remain on. Bolsonaro Stage 4 verdict quality and SVP acquisition-yield quality remain separate held lanes.

**Warnings:** Unrelated pre-existing worktree artifacts were preserved in `stash@{0}` as `pre-lane2-lane3-promotion-unrelated-worktree`: a daily-scan `Agent_Outputs.md` addition, `Docs/WIP/2026-05-04_Lane2_Lane3_Promotion_External_Review_Prompts.md`, and `diff.patch`. They were not included in the promotion. Local `main` is ahead of `origin/main`; no push was performed.

**For next agent:** Continue from local `main@78596f5d` plus the completion-doc commit if present. Treat the release payload as Lane 2 observability/provenance plus Lane 3 selected-claim admission/coverage only; do not infer Stage 4 repairs, prompt edits, prepared-routing/applicability, 15-minute idle default, or private/legal docs from this promotion.

**Learnings:** No `Role_Learnings.md` update. The transient `drain-runner-pause.integration.test.ts` broad-suite timeout was already known from the review packet and did not produce a new role-level operating rule.
