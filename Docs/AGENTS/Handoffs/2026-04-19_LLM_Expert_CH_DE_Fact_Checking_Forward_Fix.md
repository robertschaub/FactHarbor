### 2026-04-19 | LLM Expert | Codex (GPT-5) | CH-DE Fact-Checking Forward Fix
**Task:** Investigate why `Die Schweiz hat kein systematisches Fact-Checking wie Deutschland` regressed from recent successful runs to `UNVERIFIED` in job `7ff65fb0557d483e84c9192bef141998`, identify the git-caused change, and implement a better fix that still addresses the earlier `4c5218a2960444c29baccff13f21cb38` false-positive concern.

**Files touched:** `apps/web/prompts/claimboundary.prompt.md`, `apps/web/test/unit/lib/analyzer/verdict-prompt-contract.test.ts`, `Docs/AGENTS/Handoffs/2026-04-19_LLM_Expert_CH_DE_Fact_Checking_Forward_Fix.md`, `Docs/AGENTS/Agent_Outputs.md`

**Key decisions:** The regression window is now clear. Exact-input runs `a98ec095...` and `02dc8880...` on commit `8f3ca9dd...` / prompt hash `ea82aeb0...` were strong (`MOSTLY-TRUE 73/55`, `MOSTLY-TRUE 72/55`). The later run `4c5218a2...` on prompt hash `8298884f...` stayed above `UNVERIFIED` (`LEANING-TRUE 58/32`) because one-sided Germany ecosystem evidence plus Swiss proxy/omission material was allowed to settle above unresolved. The current failing run `7ff65fb0...` on `fc67d09f...` / prompt hash `644254e1...` collapsed to `UNVERIFIED 50/23`. Comparing git history between the last strong success and current HEAD showed no relevant analyzer code changes in `research-query-stage.ts`, `research-orchestrator.ts`, `verdict-generation-stage.ts`, or `claim-extraction-stage.ts`; the only material git-caused change in the implicated runtime path is prompt edit `1e206930` in `claimboundary.prompt.md`, which added the strict comparative-ecosystem `UNVERIFIED` scaffold. However, the current bad run is not explained by that verdict rule alone: its Stage 2 source mix is also weaker than the strong recent successes, and that weaker mix is not explained by a git diff. I therefore implemented a forward fix instead of a revert:

1. **Stage 2 query generation:** comparative institutional/ecosystem claims must now include at least one **enumerative ecosystem route per side** (directory/registry, participant or certification list, network roster, dedicated unit page, recurring ecosystem report), and gap-follow-up queries must pivot the weaker side toward those enumerative routes instead of repeating broad topical queries.
2. **Stage 2 extraction:** for institutional-ecosystem claims, evidence is no longer demoted to mere context just because each source covers one actor or one umbrella network page. Multiple source-native actor/network pages can collectively evidence the broader ecosystem.
3. **Stage 4 reconciliation:** replaced the blunt per-side direct-evidence rule with a narrower rule: one-sided proxy/context asymmetry still cannot settle the comparison above `UNVERIFIED`, but the model must **not** force `UNVERIFIED` solely because no single formal registry/audit exists when multiple convergent close-ecosystem sources document participants, memberships, dedicated units, recurring outputs, or governance structures.

This keeps the earlier `4c5218...` concern addressed while reopening the path for the stronger multi-source, close-ecosystem evidence patterns seen in successful runs like `02dc8880...`.

**Verification:** Focused prompt contract checks passed after the forward fix:
- `npm -w apps/web run test -- test/unit/lib/analyzer/verdict-prompt-contract.test.ts test/unit/lib/analyzer/prompt-frontmatter-drift.test.ts`
- Result: `74 passed`

Prompt rollout is active: `npm -w apps/web run reseed:prompts` updated `claimboundary` from `d2d5547f...` to active hash `5b34870a0258dc2104294c1babdde0894c9b5e8d2d11c1d79d3382723debcd66` (`activated_utc: 2026-04-19T15:15:51.356Z`).

**Open items:** A fresh post-fix rerun was submitted as job `df612169dab34eb788beb66384ace691`, but it never left `QUEUED` during this run, so there is not yet a completed live report proving the behavioral improvement. The current worktree is also still dirty because the forward fix is not committed yet.

**Warnings:** The post-fix live verification is blocked by local runner/infrastructure state rather than by the prompt tests. During this run, recent jobs showed backlog plus unrelated runner problems in `apps/web/debug-analyzer.log` (`API GET 404`, one `API PUT 401`, and Anthropic credit/model failures on other jobs). Another job (`b1d3d49f...`) is still `RUNNING`, and `df612169...` remained queued behind it.

**For next agent:** Wait for or unblock job `df612169dab34eb788beb66384ace691`, then compare it against `7ff65fb0557d483e84c9192bef141998`, `4c5218a2960444c29baccff13f21cb38`, and `02dc888046844002a0c3f4d25caa89df`. Primary anchors for the implemented forward fix are [claimboundary.prompt.md](/c:/DEV/FactHarbor/apps/web/prompts/claimboundary.prompt.md) in `GENERATE_QUERIES`, `EXTRACT_EVIDENCE`, and `VERDICT_RECONCILIATION`, plus [verdict-prompt-contract.test.ts](/c:/DEV/FactHarbor/apps/web/test/unit/lib/analyzer/verdict-prompt-contract.test.ts). The key historical attribution claim to preserve is: Stage 2 runtime variance is real, but the only relevant git-caused regression in the analyzed window is the stricter Stage 4 comparative-ecosystem rule introduced after the `8f3ca9dd...` success runs.

**Learnings:** For this family, a prompt-only verdict hardening can look like the sole regression cause when it is actually interacting with retrieval variance. When the git diff is narrow but the evidence pool is also weaker, the safer forward fix is to narrow the verdict guard and simultaneously strengthen upstream query/extraction guidance rather than reverting either side blindly.
