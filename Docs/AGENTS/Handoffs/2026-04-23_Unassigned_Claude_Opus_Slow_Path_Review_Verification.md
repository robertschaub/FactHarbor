---
### 2026-04-23 | Unassigned | Codex (GPT-5) | Claude Opus Slow Path Review Verification
**Task:** Verify Claude Opus's review of the claim-selection dialog slow-path diagnosis against the current code.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-23_Unassigned_Claude_Opus_Slow_Path_Review_Verification.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Confirmed the main root-cause framing: Stage 1 preparation blocks recommendation, and contract-validation retry cost plus weak progress signaling are the dominant contributors. Corrected one important overstatement: recommendation timing is instrumented in code, but draft preparation does not run inside the `runWithMetrics(...)` AsyncLocalStorage scope that persists `recordLLMCall(...)`, so recommendation timing is not proven queryable for draft prep. Also confirmed a larger observability gap than noted in the review: the draft status API accepts `eventMessage`, but `ClaimSelectionDraftService.UpdateStatusAsync(...)` does not persist it, and the public draft GET/UI do not expose it.
**Open items:** Add per-step Stage 1 timing around contract validation, retry Pass 2, retry validation, and repair; persist and expose draft status milestone text; then decide whether recommendation optimization is warranted after measuring real wall-clock share on 5+ claim drafts.
**Warnings:** Do not assume draft-preparation LLM metrics already exist in the persisted metrics table. Do not limit the observability fix to progress normalization alone; backend status-message persistence is also missing. Recommendation cost is skipped entirely for drafts with 1-4 candidate claims because auto-continue bypasses the recommendation step.
**For next agent:** Start from `apps/web/src/lib/internal-runner-queue.ts` (`runDraftPreparationBackground`, `normalizeDraftPreparationProgress`), `apps/web/src/lib/analyzer/claim-extraction-stage.ts` (contract validation / retry / repair path), `apps/web/src/lib/analyzer/claim-selection-recommendation.ts` (`recordLLMCall(...)`), `apps/web/src/lib/analyzer/metrics-integration.ts` (`runWithMetrics(...)`, `recordLLMCall(...)`), `apps/api/Services/ClaimSelectionDraftService.cs` (`UpdateStatusAsync` ignores `eventMessage`), and `apps/web/src/app/analyze/select/[draftId]/page.tsx` (2s polling, percentage-only UI).
**Learnings:** no
