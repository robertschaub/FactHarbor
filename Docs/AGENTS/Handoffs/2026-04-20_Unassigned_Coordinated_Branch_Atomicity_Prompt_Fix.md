### 2026-04-20 | Unassigned | Codex (GPT-5) | Coordinated Branch Atomicity Prompt Fix
**Task:** Fix the new Stage-1 atomicity regression where a coordinated temporal claim like `... bevor Volk und Parlament ...` was being forced into one non-atomic claim instead of proper independently verifiable branches.
**Files touched:** `apps/web/prompts/claimboundary.prompt.md`; `apps/web/test/unit/lib/analyzer/claim-extraction-prompt-contract.test.ts`; `Docs/AGENTS/Handoffs/2026-04-20_Unassigned_Coordinated_Branch_Atomicity_Prompt_Fix.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** The previous `Compound subject/object exception` was too strong. It prevented the model from decomposing coordinated participants even when the branches were independently verifiable. The prompt now uses a `Coordinated branch rule` instead: split only when the branches are independently verifiable, preserve the shared temporal/conditional anchor in each branch, and never keep the unsplit whole sentence alongside branch claims. The contract-repair section was also tightened so it no longer merges coordinated branches back into a single claim merely because they share one anchor.
**Verification:** `npm -w apps/web exec vitest run test/unit/lib/analyzer/claim-extraction-prompt-contract.test.ts` passed (`11 passed`).
**Open items:** The running stack still needs a restart to pick up the prompt change in `config.db`, and the queued clean Bundesrat rerun should then be used as the runtime confirmation. Until that rerun finishes, this remains a prompt-level fix with unit-contract validation only.
**Warnings:** A previously completed Bundesrat job (`820600477bc4461a826c81dfe8730ef3`) showed the older over-merged shape on `403e905a+dirty`; that result will not update automatically. A queued clean rerun (`d540276e9e8343b0bf4a08b92b698d36`) exists and should be allowed to run after restart.
**For next agent:** Start with `apps/web/prompts/claimboundary.prompt.md` around the Pass 1 / Pass 2 classification bullets and `CLAIM_CONTRACT_REPAIR`. The key contract is now: coordinated branches may be split when independently verifiable, but the whole unsplit sentence must not coexist with those branch claims, and repair must not merge them back together.
**Learnings:** no

