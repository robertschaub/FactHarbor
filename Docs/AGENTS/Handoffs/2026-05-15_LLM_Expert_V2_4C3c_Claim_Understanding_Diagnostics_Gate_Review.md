---
### 2026-05-15 | LLM Expert | Codex (GPT-5) | V2 4C3c Claim Understanding Diagnostics Gate Review
**Task:** Evaluate whether the post-4C3c `claim_contract_validation_failed` result provides enough evidence to edit the Claim Understanding prompt/schema/model, or whether hidden schema-error diagnostics are needed first.
**Files touched:** `Docs/AGENTS/Handoffs/2026-05-15_LLM_Expert_V2_4C3c_Claim_Understanding_Diagnostics_Gate_Review.md`, `Docs/AGENTS/Agent_Outputs.md`.
**Key decisions:** Verdict is diagnostics first. 4C3c proved hidden direct-text runtime reachability and no public leakage, but the accepted smoke artifact exposes only `schemaOutcome: damaged` with `damagedReason: claim_contract_validation_failed`. The adapter already records attempt-level parse/schema failure messages internally, but the hidden runtime artifact drops those details for completed dispatch, so the failure cannot yet be attributed to prompt wording, schema mismatch, model capability, provider output mode, or runtime variable binding.
**Open items:** Add a reviewed, internal-only hidden diagnostics gate that captures sanitized adapter attempt status and schema issue paths/messages without raw secrets or public exposure. Only after that gate is committed/refreshed should a single Captain-defined direct-text smoke be considered if needed to collect the diagnostic evidence.
**Warnings:** Do not edit `apps/web/prompts/claimboundary-v2.prompt.md`, `apps/web/src/lib/analyzer-v2/claim-understanding/schemas.ts`, model policy, or provider/runtime behavior based only on the current coarse damaged reason. Prompt/config/model/schema changes still require separate review and topic-neutral wording. No broader live jobs are justified from this evidence.
**For next agent:** Start from `apps/web/src/lib/analyzer-v2/claim-understanding/model-adapter.ts`, where `attempts[].failureMessage` records parse/schema failures, and `apps/web/src/lib/analyzer-v2/claim-understanding/runtime-stage.ts`, where `recordRuntimeArtifact(...)` currently writes completed dispatch artifacts with `failureMessage: null`. Keep diagnostics internal-admin-only under `v2_observability_ledger`; preserve public leakage guards.
**Learnings:** No durable role learning appended.
