---
### 2026-03-29 | Senior Developer | Codex (GPT-5) | b8e6 Duplicate Atomic Claims Investigation
**Task:** Investigate why job `b8e616ef9a5e4678b074f2bb8614b2d1` produced extremely similar atomic claims and determine whether a recent change caused it.
**Files touched:** `Docs/AGENTS/Handoffs/2026-03-29_Senior_Developer_b8e6_Duplicate_Atomic_Claims_Investigation.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** The root cause is Stage 1, not a recent verdict/research regression. The stored run shows `understanding.inputClassification = "single_atomic_claim"` but Pass 2 still emitted two near-duplicate claims for `Werkzeuge` and `Methoden`. Those claims were then auto-tagged `isDimensionDecomposition = true` by the fallback heuristic in `apps/web/src/lib/analyzer/claim-extraction-stage.ts` because both had `centrality = high` and `claimDirection = supports_thesis`. That heuristic exempts them from Gate 1 fidelity filtering. Contract validation did not catch the over-split; for this job it incorrectly summarized the extraction as preserving the original claim contract without decomposition. Recent committed changes in the last 3 days do not explain this run: the executed base commit `f1e5cc963849cb2745613eb6ca0f253e68e2a144` is UI-only, the only committed Stage-1 change in the last 3 days was observability (`d6090f7`, no behavior change), and the fallback heuristic itself was introduced earlier on 2026-03-23 (`81150d385`). Recent prompt edits in the last 3 days touched verdict/research sections, not the claim-extraction rules.
**Open items:** The exact dirty-worktree delta from execution build `f1e5cc96...+cbdf73b6` cannot be reconstructed from the stored job alone, so uncommitted local changes at run time cannot be ruled out with absolute certainty. However, there is no evidence in committed changes that a last-3-day regression caused this Stage-1 split.
**Warnings:** This is not a one-off. In the most recent 200 succeeded jobs, 7 runs had `inputClassification = single_atomic_claim` while still producing multiple `isDimensionDecomposition` claims. Examples include Bolsonaro runs from 2026-03-26 to 2026-03-28 and the SRG SSR run here. That points to a broader fallback-heuristic / contract-validation gap rather than an isolated bad prompt sample.
**For next agent:** Primary anchors are `apps/web/src/lib/analyzer/claim-extraction-stage.ts` around the dimension-decomposition tagging block and Gate 1 fidelity exemption, plus the extraction rules in `apps/web/prompts/claimboundary.prompt.md` around `single_atomic_claim`, `ambiguous_single_claim`, and semantic-overlap merging. If fixing this class, focus first on the `single_atomic_claim` fallback tagging path and the contract-validator false-negative, not on any last-3-day verdict changes.
**Learnings:** no
