---
### 2026-04-21 | Senior Developer | GitHub Copilot (GPT-5.4) | Unverified Validator Retry Hardening
**Task:** Investigate the new `UNVERIFIED` jobs on commit `128414e4`, identify the concrete failure path, and harden the Stage 1 contract-validator path without widening into speculative prompt work.
**Files touched:** `apps/web/src/lib/analyzer/claim-extraction-stage.ts`, `apps/web/test/unit/lib/analyzer/claim-contract-validation.test.ts`, `Docs/AGENTS/Handoffs/2026-04-21_Senior_Developer_Unverified_Validator_Retry_Hardening.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** The three `UNVERIFIED` jobs split into two failure classes. `d433c56e...` and `bc6325e6...` are `validator_unavailable` Stage 1 aborts; `4efca867...` is a real contract-violation path on the PDF/article input. The landed code fix addresses the validator-unavailable class by adding `runClaimContractValidationWithRetry(...)` and using it at every Stage 1 contract-validation seam (initial validation, retry validation, repair validation, final revalidation). This keeps the existing anchor-guided retry/repair machinery alive when the first structured-output attempt is transiently empty. I also removed the exact-match anchor reordering inside `buildContractRetrySaliencePlan(...)`; if the validator anchor already exists in the authoritative salience inventory, the helper now preserves upstream order instead of promoting that anchor to the front.
**Open items:** I did not run fresh live reruns. The residual PDF/article regression is not proven solved by this patch. `4efca867...` remains a separate report-review item if the next rerun still fails: inspect whether the retry claim set is materially worse or whether another validator-unavailable event is hiding recovery. `bc6325e6...` may also still fail if its final revalidation stays unavailable after two attempts; this patch improves initial/retry/repair resilience more than the already-retried final path.
**Warnings:** The repo already had unrelated dirty docs/index changes (`Docs/AGENTS/Agent_Outputs.md`, `Docs/AGENTS/index/stage-map.json`, `Docs/AGENTS/index/stage-manifest.json`, `Docs/WIP/README.md`, plus WIP handoff files). I left those untouched except for appending the required Agent_Outputs entry. Do not treat the remaining PDF/article failure as evidence that the validator retry helper is wrong; it is a distinct path until a rerun proves otherwise.
**For next agent:** Start with [apps/web/src/lib/analyzer/claim-extraction-stage.ts](apps/web/src/lib/analyzer/claim-extraction-stage.ts#L348), [apps/web/src/lib/analyzer/claim-extraction-stage.ts](apps/web/src/lib/analyzer/claim-extraction-stage.ts#L2602), and [apps/web/src/lib/analyzer/claim-extraction-stage.ts](apps/web/src/lib/analyzer/claim-extraction-stage.ts#L3153). The focused proof is in [apps/web/test/unit/lib/analyzer/claim-contract-validation.test.ts](apps/web/test/unit/lib/analyzer/claim-contract-validation.test.ts#L247) and [apps/web/test/unit/lib/analyzer/claim-contract-validation.test.ts](apps/web/test/unit/lib/analyzer/claim-contract-validation.test.ts#L450). Exact job evidence used in this investigation: `d433c56e1a4e49a1a4d3b2fcfa1e35ed` (bad same-input sibling of `01730e882ba84173ad278ce26dd6af96`), `bc6325e68af746aba28c5eced78ae773`, and `4efca867b0c64498b01d916827800c9c`.
**Learnings:** Appended to Role_Learnings.md? **No** — this is a repo-specific analyzer hardening and is captured in repo memory instead.
