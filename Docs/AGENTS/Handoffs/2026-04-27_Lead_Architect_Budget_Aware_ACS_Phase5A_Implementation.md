---
### 2026-04-27 | Lead Architect | Codex (GPT-5) | Budget-Aware ACS Phase 5A Implementation
**Task:** Implement the low-risk, reviewed Phase 5A subset for budget-aware Atomic Claim Selection metadata without enabling prompt behavior.
**Files touched:** `apps/web/src/lib/claim-selection-flow.ts`; `apps/web/src/lib/analyzer/types.ts`; `apps/web/src/lib/analyzer/claim-selection-recommendation.ts`; `apps/web/src/lib/config-schemas.ts`; `apps/web/configs/pipeline.default.json`; `apps/web/src/lib/internal-runner-queue.ts`; `apps/api/Services/JobService.cs`; `apps/web/src/app/analyze/select/[draftId]/page.tsx`; `apps/web/src/app/analyze/select/[draftId]/ClaimSelectionPanel.tsx`; `apps/web/src/app/analyze/select/[draftId]/page.module.css`; `apps/web/src/app/admin/preparation/[draftId]/page.tsx`; focused unit tests under `apps/web/test/unit/lib/`.
**Key decisions:** A debate reviewed the risk and returned `MODIFY`: proceed only with guarded schema/types/persistence/UI/API plumbing. Added default-off UCM fields `claimSelectionBudgetAwarenessEnabled`, `claimSelectionBudgetFitMode`, and `claimSelectionMinRecommendedClaims`; added optional `deferredClaimIds`, `budgetFitRationale`, and per-claim `budgetTreatment` metadata; persisted metadata through draft state and final API job JSON; disclosed deferred/budget rationale in selection and admin preparation views when present. No prompt section was changed, and no normal run behavior is enabled.
**Open items:** Phase 5B still needs explicit approval before prompt behavior is enabled. It must pass budget context to the ACS recommendation prompt, require LLM-generated budget-fit/defer semantics, and validate multilingual behavior on Captain-approved inputs. Operational validation can use UCM `claimSelectionDefaultMode=automatic`, but that is setup, not product behavior.
**Warnings:** `npm test` full safe suite timed out twice in `test/unit/lib/runner-concurrency-split.integration.test.ts > emits a heartbeat when a report job has no recent stage event`; that exact file/test passes in isolation, and the Phase 5A touched runner queue tests passed. Treat this as a residual suite-timing risk, not as evidence that the budget metadata patch is broken. Normal API build was blocked by a running `FactHarbor.Api` process locking `bin/Debug/net8.0`; isolated-output `dotnet build` passed.
**For next agent:** Do not add a second selector, deterministic semantic filtering, hidden claim caps, or final-runner dropping. The new metadata is inert until prompt behavior is deliberately enabled. If enabling Phase 5B, keep `claimSelectionCap` as an upper bound and make every fewer-than-cap budget recommendation explicit, LLM-generated, persisted, and visible.
**Learnings:** No `Role_Learnings.md` append; the reusable learning is already captured in the prior Slice 5 design handoff: ACS had structural room for fewer-than-cap recommendations, but lacked audit/disclosure metadata.

```text
DEBT-GUARD RESULT
Classification: missing audited contract capability, with failed-validation recovery required after full-suite timeout.
Chosen option: add default-off metadata plumbing and validation to the existing ACS recommendation path.
Rejected paths: enabling prompt behavior now, adding deterministic budget-based claim filtering, changing global caps, or broadening into unrelated runner-test refactors.
Failed-validation recovery: keep the implementation; the repeated full-suite timeout is isolated to a runner concurrency test that passes alone, while touched focused tests pass.
Verification: focused claim-selection recommendation tests; claim-selection-flow/config/config-drift tests; internal-runner-queue and claim-selection draft route tests; runner concurrency test in isolation; web build; isolated-output API build; git diff --check.
```
