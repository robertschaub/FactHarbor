---
### 2026-03-23 | Senior Developer | Codex (GPT-5) | Stage 2 Slice 1 State Utils Extraction
**Task:** Start the approved Stage 2 deconstruction with the smallest safe slice by extracting pure State-2 targeting/sufficiency/budget helpers out of `claimboundary-pipeline.ts`.
**Files touched:** `apps/web/src/lib/analyzer/claimboundary-pipeline.ts`, `apps/web/src/lib/analyzer/research-orchestrator.ts`, `Docs/WIP/2026-03-23_Stage_2_Research_Loop_Deconstruction_Design.md`, `Docs/AGENTS/Handoffs/2026-03-23_Senior_Developer_Stage2_Slice1_State_Utils.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Created `research-orchestrator.ts` as the first Stage 2 module and moved only the pure/near-pure helper surface into it: claim targeting (`findLeastResearchedClaim`, `findLeastContradictedClaim`), sufficiency (`allClaimsSufficient`), and per-claim query-budget helpers (`getPerClaimQueryBudget`, `getClaimQueryBudgetUsed`, `getClaimQueryBudgetRemaining`, `consumeClaimQueryBudget`). Kept `researchEvidence`, `runResearchIteration`, preliminary evidence seeding, source reconciliation, and all LLM/search logic in `claimboundary-pipeline.ts`. Preserved the public import surface through re-exports from `claimboundary-pipeline.ts`, so existing tests/imports did not move.
**Open items:** Slice 2 is still pending and should be a separate approval/implementation step. The remaining Stage 2 core in `claimboundary-pipeline.ts` is still the async research loop plus query/acquisition/extraction behavior.
**Warnings:** This slice deliberately does not yet make `research-orchestrator.ts` the owner of the loop itself. The file currently contains Stage 2 state/budget helpers only; do not treat it as approval to continue into query/acquisition extraction without a new decision. There are unrelated working-tree changes in `apps/web/package.json`, `apps/web/vitest.config.ts`, `apps/web/src/lib/search-provider-utils.ts`, `apps/web/test/integration/hydrogen-smoke.test.ts`, and `scripts/submit-hydrogen-job.ps1`; they were left untouched.
**For next agent:** Use `research-orchestrator.ts` as the anchor for future Stage 2 work. The safe next cut is likely Slice 2 from the design doc (`research-extraction-stage.ts`) or another explicitly approved narrow Stage 2 boundary. Verification completed for this slice: `npm -w apps/web run test -- test/unit/lib/analyzer/claimboundary-pipeline.test.ts` and `npm -w apps/web run build`.
**Learnings:** no
