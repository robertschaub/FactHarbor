---
### 2026-04-23 | Unassigned | Codex (GPT-5) | Claim Selection Slow Path Implementation Plan Review
**Task:** Turn the ACS slow-path diagnosis into a concrete implementation plan, then review that plan with a structured debate.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-23_Unassigned_Claim_Selection_Slow_Path_Implementation_Plan_Review.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** The reviewed baseline is `MODIFY`, not pure adopt/reject. Keep the instrumentation-first approach, but make `ClaimSelectionDraftState.observability` the authoritative draft-preparation telemetry contract. Use a row-level `LastEventMessage` only as live UI convenience state. The first slice should therefore: (1) persist and expose the latest draft milestone text for live feedback; (2) persist authoritative draft observability in `DraftStateJson` with phase identity and separated timings; (3) split the Stage 1 contract-validation / retry / repair plateau into truthful positive progress milestones; and (4) defer recommendation optimization plus polling/SSE changes until post-patch timings show they matter. The strongest counterargument survived in narrowed form: avoid letting the row field become a second long-lived telemetry source of truth.
**Open items:** Exact observability shape still needs to be finalized before implementation. Current recommended fields are `phaseCode`, `phaseLabel`, `eventMessage`, `branch` (`clean|retry|repair`), `stage1Ms`, `recommendationMs`, `totalPrepMs`, and sub-step timings for contract validation / retry / repair. Still open: whether `LastEventUtc` is needed, what API/public response shape should expose the derived preparation block, and what minimal API test seam can be added for draft-status persistence.
**Warnings:** The validator rated the plan `PASS-WITH-CAVEATS`. Main risks are: schema-upgrade behavior for a new draft column, absence of an established draft-status persistence test seam, truthful milestone wording requirements, and the possibility that draft-local observability hardens into a parallel telemetry system if no later unification decision is made. Recommendation remains skipped for 1-4 claim drafts, so do not treat recommendation timing as the primary acceptance signal.
**For next agent:** Implement from these anchors: `apps/web/src/lib/internal-runner-queue.ts` for draft orchestration and recommendation timing; `apps/web/src/lib/analyzer/claim-extraction-stage.ts` for Stage 1 milestone splitting and sub-step timings; `apps/web/src/lib/analyzer/types.ts` for `ClaimSelectionDraftState.observability`; `apps/api/Data/Entities.cs`, `apps/api/Services/ClaimSelectionDraftService.cs`, `apps/api/Controllers/ClaimSelectionDraftsController.cs`, and `apps/api/Program.cs` for row-level `LastEventMessage` plus column-upgrade logic; `apps/web/src/app/analyze/select/[draftId]/page.tsx` for rendering the latest milestone text and any derived preparation state. Acceptance target: a 5+ claim draft shows live milestone text during the slow Stage 1 window, stores separated draft-prep timings, and no longer appears as a generic flat `24%` wait without context.
**Learnings:** no
