---
### 2026-04-19 | Unassigned | Codex (GPT-5) | Prompt Audit Schema Contract Findings
**Task:** Review `input-policy-gate.prompt.md`, `source-reliability.prompt.md`, and `text-analysis/inverse-claim-verification.prompt.md` against their real runtime parsers/schemas and judge three disputed audit claims.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-19_Unassigned_Prompt_Audit_Schema_Contract_Findings.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** `input-policy-gate` contract concern is weakened because runtime already validates and sanitizes all returned fields before use; `source-reliability.prompt.md` itself is mostly schema-compatible but the real live seam is weaker prompt-file relevance plus permissive runtime schemas that do not enforce the prompt’s promised enums/caps; missing frontmatter on `inverse-claim-verification.prompt.md` is rejected as a current-runtime issue because the file is read directly from disk by `paired-job-audit.ts`, not via prompt-loader/UCM validation.
**Open items:** If Captain wants a follow-up patch, the highest-value fix is SR contract hardening: enforce `sourceType`/bias enums in `sr-eval-types.ts` or normalize them before cap enforcement, and decide whether the live SR evaluator should actually load `source-reliability.prompt.md` instead of duplicating prompt text in `sr-eval-prompts.ts`.
**Warnings:** `paired-job-audit.ts` comments still call the inverse verifier prompt “UCM-managed” even though current code bypasses UCM and frontmatter validation entirely.
**For next agent:** Key anchors are `apps/web/src/lib/input-policy-gate.ts:113-146`, `apps/web/src/app/api/fh/analyze/route.ts:31-38`, `apps/web/src/lib/source-reliability/sr-eval-types.ts:63-92`, `apps/web/src/lib/source-reliability/sr-eval-engine.ts:128-145`, `apps/web/src/lib/source-reliability/sr-eval-prompts.ts:503-520`, `apps/web/src/lib/config-storage.ts:1277-1283`, and `apps/web/src/lib/calibration/paired-job-audit.ts:159-183`.
**Learnings:** no
