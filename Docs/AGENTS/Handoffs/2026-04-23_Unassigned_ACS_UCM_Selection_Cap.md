### 2026-04-23 | Unassigned | Codex (GPT-5) | ACS UCM Selection Cap
**Task:** Replace the remaining hardcoded ACS `5`-claim limits with one UCM-controlled cap and make recommendation count dynamic under that same limit.
**Files touched:** `apps/web/src/lib/claim-selection-flow.ts`; `apps/web/src/lib/config-schemas.ts`; `apps/web/configs/pipeline.default.json`; `apps/web/src/lib/analyzer/claim-selection-recommendation.ts`; `apps/web/src/lib/analyzer/types.ts`; `apps/web/src/lib/internal-runner-queue.ts`; `apps/web/src/app/analyze/select/[draftId]/page.tsx`; `apps/web/src/app/api/fh/claim-selection-drafts/[draftId]/confirm/route.ts`; `apps/api/Services/ClaimSelectionDraftService.cs`; `apps/api/Services/JobService.cs`; `apps/web/test/unit/lib/claim-selection-flow.test.ts`; `apps/web/test/unit/lib/analyzer/claim-selection-recommendation.test.ts`; `apps/web/test/unit/app/api/fh/claim-selection-drafts-routes.test.ts`.
**Key decisions:** Added `pipeline.claimSelectionCap` as the single UCM knob for the current ACS `5`-based behavior. The shared flow helpers now derive auto-continue (`< cap`), manual-review threshold (`>= cap`), and effective selection/recommendation cap from that value. The runner persists the configured threshold into `ClaimSelectionDraftState.selectionCap`, the recommendation module uses that threshold to compute its dynamic max recommendation count, the selection page recomputes the effective current limit from candidate count plus persisted threshold, and API confirm validation now does the same instead of trusting the stored threshold directly. `JobService` now also carries `selectionCap` into persisted `ClaimSelectionMetadata` so downstream draft-backed jobs retain the same selection contract. The Next proxy intentionally stopped enforcing the upper bound so the persisted draft state remains the runtime authority.
**Open items:** None for this slice.
**Warnings:** Reviewer follow-up found one real mismatch: API confirm originally validated against the persisted threshold instead of the effective current cap; this slice fixes that by deriving `min(candidateCount, selectionCap)` on confirm. Legacy drafts without persisted `selectionCap` still fall back to the pre-change cap of `5` as a compatibility path only. A plain `dotnet build` against `apps/api` still fails when the live dev process holds `bin\\Debug\\net8.0\\FactHarbor.Api.exe`; use `dotnet build -o <temp-dir>` or stop the running API first.
**For next agent:** If ACS selection behavior changes again, update the same four authority points together: `pipeline.claimSelectionCap` in [config-schemas.ts](/c:/DEV/FactHarbor/apps/web/src/lib/config-schemas.ts), persisted draft state in [internal-runner-queue.ts](/c:/DEV/FactHarbor/apps/web/src/lib/internal-runner-queue.ts), UI behavior/copy in [page.tsx](/c:/DEV/FactHarbor/apps/web/src/app/analyze/select/[draftId]/page.tsx), and confirm enforcement in [ClaimSelectionDraftService.cs](/c:/DEV/FactHarbor/apps/api/Services/ClaimSelectionDraftService.cs). Recommendation cap logic now lives in [claim-selection-recommendation.ts](/c:/DEV/FactHarbor/apps/web/src/lib/analyzer/claim-selection-recommendation.ts).
**Learnings:** No role learning update.
