---
### 2026-04-15 | Unassigned | Codex (GPT-5) | Salience Model-Tier Design Review
**Task:** Review the salience model-tier/design question in the Stage 1 analyzer path and recommend the safest next step for improving salience quality without changing other shared understand/extract lanes.
**Files touched:** `Docs/AGENTS/Handoffs/2026-04-15_Unassigned_Salience_Model_Tier_Design_Review.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Current salience routing is intentionally wired through `getModelForTask("understand")` in `runSalienceCommitment`, but changing `modelUnderstand` would also change Pass 1, Gate 1, query generation, relevance/applicability, seeded-evidence remap, and scope normalization. Reusing `extract_evidence` would also be broad because Pass 2 and evidence extraction already depend on that lane. The recommended next step is a dedicated UCM-backed salience task/model key in pipeline config, defaulted to the current budget behavior for a no-op rollout, while leaving `salienceCommitment.enabled/mode` in calculation config.
**Open items:** Decide whether per-task isolation is only required when `llmTiering=true` or also when tiering is disabled. If the tuned salience model should be visible in calibration/admin outputs, extend those reporting surfaces in the same slice instead of hiding the new lane behind runtime-only behavior.
**Warnings:** A silent switch to `context_refinement` would couple salience to `modelVerdict` and contract-validation/verdict-adjacent behavior rather than isolating it. Do not combine the routing change with prompt edits if the team wants a clean salience-quality experiment.
**For next agent:** Primary code refs are `apps/web/src/lib/analyzer/claim-extraction-stage.ts` (`runSalienceCommitment`, Pass 2, Gate 1), `apps/web/src/lib/analyzer/llm.ts` (task-to-model routing), `apps/web/src/lib/config-schemas.ts` + `apps/web/configs/pipeline.default.json` (pipeline model knobs), and `apps/web/configs/calculation.default.json` (salience audit/binding mode). Secondary blast-radius refs are `apps/web/src/app/admin/config/page.tsx` and `apps/web/src/lib/calibration/{types.ts,runner.ts,report-generator.ts}`.
**Learnings:** no
