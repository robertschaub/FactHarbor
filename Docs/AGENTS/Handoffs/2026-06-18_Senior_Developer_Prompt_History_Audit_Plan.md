---
### 2026-06-18 | Senior Developer | Codex (GPT-5) | Prompt History Audit + Reviewed Cleanup Plan
**Task:** Investigate prompt history before any general prompt audit or prompt/code/UCM cleanup, then consolidate reviewed execution candidates.
**Files touched:** `Docs/AGENTS/Handoffs/2026-06-18_Senior_Developer_Prompt_History_Audit_Plan.md`, `Docs/AGENTS/Agent_Outputs.md`.
**Key decisions:** Read-only audit only. Used `prompt-audit`, Huygens and James sidecars, plus Claude/Gemini wrapper reviews. Consensus: do not start by pruning `claimboundary.prompt.md`; first address concrete prompt/runtime ownership mismatches and code/UCM enablers for the two live failures. `CLAIM_CONTRACT_VALIDATION`, salience/binding, `CLAIM_SINGLE_CLAIM_ATOMICITY_VALIDATION`, and `CLAIM_CONTRACT_SURGICAL_REPAIR` are load-bearing or not yet measured enough to drop blindly. Already-reverted prompt fixes must not be resurrected without a new mechanism.
**Open items:** P0 candidates are source-reliability prompt ownership, input-policy-gate schema alignment, generic source-native retrieval lane, and decisive current-primary-metric sufficiency path. Broad ClaimBoundary consolidation should wait until F2/report_damaged measurement is not confounded.
**Warnings:** The current Bolsonaro/Asylum failures are not prompt-cleanup-only failures. Prompt-only edits would likely add pile-up because runtime lanes are only `primary | supplementary_en`, and D5/Stage 4 need code/UCM support for decisive metric admission. If the next edit changes bugfix behavior, start with failed-attempt recovery from baseline `0396ea47`.
**For next agent:** Start with P0-A: inspect `apps/web/src/lib/source-reliability/sr-eval-engine.ts`, `apps/web/src/lib/source-reliability/sr-eval-prompts.ts`, `apps/web/src/lib/source-reliability/sr-eval-enrichment.ts`, and `apps/web/prompts/source-reliability.prompt.md`; choose a single prompt owner or explicitly mark the Markdown prompt as EQA-only. Keep `claimboundary.prompt.md` pruning as a later selective-prune track.
**Learnings:** Appended to Role_Learnings.md? no.
