---
### 2026-04-20 | Unassigned | Codex (GPT-5) | Unverified Stage 1 Contract Stability Fix
**Task:** Fix the `/report-review` regression behind recent `UNVERIFIED` jobs on the reports list.
**Files touched:** `apps/web/src/lib/analyzer/claim-extraction-stage.ts`; `apps/web/prompts/claimboundary.prompt.md`; `apps/web/test/unit/lib/analyzer/claim-extraction-prompt-contract.test.ts`; `apps/web/test/unit/lib/analyzer/contract-revalidation-fallback.test.ts`
**Key decisions:** Treated the screenshot as a real Stage 1 regression, not a UI badge bug. Tightened Stage 1 extraction/validation prompts in two generic directions: explicit conjunct coverage for multi-assertion inputs, and efficiency-dimension discipline so broad efficiency claims stay inside actual efficiency/system-boundary frames instead of drifting into operational proxies. On the runtime side, made final contract revalidation safer without fail-open semantics: claim-set equivalence is now order-insensitive, and Stage 1 may carry forward the last contract-approved summary only when Gate 1 kept a subset of previously validated claims and all validated anchor carriers still survive.
**Open items:** No live reruns were executed. The three affected reports should be resubmitted on the current stack to confirm: `8497c447cbf54ddbb11680cdab4ae906`, `a2be703ddd014cf69cdb345f0c076fb9`, and `f1afe3ad61754067bd4f1d8742bae7c6`.
**Warnings:** `npm -w apps/web run build` succeeded, but its documented `postbuild` hook reseeded prompt blobs in `apps/web/config.db`. That is expected for this repo, but it means active local prompt state changed during verification. No expensive live-analysis suites or `/validate` runs were used.
**For next agent:** The main runtime seam is in `canCarryForwardValidatedContractApproval(...)` and the final revalidation branch in `extractClaims(...)`. Prompt changes are intentionally narrow and generic; if the reruns still miss, inspect Stage 1 outputs first, especially whether the Bolsonaro fairness conjunct is still omitted or whether hydrogen still creates a proxy claim around convenience/range/refueling. Verification already passed on targeted Vitest (`72 passed` across the touched analyzer suites), `tsc --noEmit`, and `npm -w apps/web run build`.
**Learnings:** no
