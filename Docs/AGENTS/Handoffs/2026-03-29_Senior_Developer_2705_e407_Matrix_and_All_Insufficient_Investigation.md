---
### 2026-03-29 | Senior Developer | Codex (GPT-5) | 2705 + e407 Matrix and All-Insufficient Investigation
**Task:** Investigate whether job `2705c6bf1c904306bd81a2040025024f` was only a Coverage Matrix UI issue and analyze the worse repeat run `e407cba4ac354248b21d26a4fb0ceaf7` to identify the proper prevention strategy.
**Files touched:** `Docs/AGENTS/Handoffs/2026-03-29_Senior_Developer_2705_e407_Matrix_and_All_Insufficient_Investigation.md`, `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** There are three separate layers. (1) `2705...` has a real UI rendering bug: the job page passes matrix labels from all claim verdicts while the matrix body renders only `coverageMatrix.claims`, creating ghost columns. (2) `e407...` proves a deeper pipeline flaw: when all claims are D5-insufficient, `claimboundary-pipeline.ts` falls back to `activeClaims = understanding.atomicClaims`, still runs Stage 4, then appends D5 `UNVERIFIED` fallbacks for the same claim IDs. That creates duplicate claim verdicts (`AC_01`, `AC_02` twice each) and lets the article verdict be driven by claims that should have been short-circuited to insufficient-evidence fallbacks. (3) The underlying Stage-1 factual conjunct split remains unresolved for this input family: both runs split `Werkzeuge` and `Methoden`, and contract validation still falsely reports perfect preservation.
**Open items:** The proper fix sequence is not just “fix the matrix.” It should be: (a) stop Stage 4 from running on all-insufficient claim sets; (b) fix the matrix header/body mismatch and add a small transparency cue for excluded claims; (c) reopen the residual Stage-1 factual conjunct-splitting problem as a separate Step 4 / Pass-2 or contract-validation refinement.
**Warnings:** `2705...` ran on dirty pre-`fff7a508` code; `e407...` ran on `fff7a508` plus a dirty suffix. That means `2705...` is not a clean regression test for the committed Stage-1 package, but `e407...` is still decisive for the Stage-4 orchestration bug because the duplicate verdicts are present in the stored result itself.
**For next agent:** Read this note before implementing anything around the Coverage Matrix. The immediate code hotspots are [claimboundary-pipeline.ts](C:/DEV/FactHarbor/apps/web/src/lib/analyzer/claimboundary-pipeline.ts) around the `activeClaims` fallback and insufficient-verdict append, [page.tsx](C:/DEV/FactHarbor/apps/web/src/app/jobs/[id]/page.tsx) where matrix labels are built from all `claimVerdicts`, and [CoverageMatrix.tsx](C:/DEV/FactHarbor/apps/web/src/app/jobs/[id]/components/CoverageMatrix.tsx) where headers render from `claimDisplayLabels` instead of `matrix.claims`.
**Learnings:** no
