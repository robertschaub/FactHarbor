---
### 2026-04-24 | Unassigned | Codex (GPT-5) | Verdict Citation Integrity Guard
**Task:** Implement the first executable pipeline-quality slice from live monitoring: prevent invalid or non-directional evidence citations from surviving in final verdict citation buckets.
**Files touched:** `apps/web/src/lib/analyzer/verdict-stage.ts`, `apps/web/src/lib/analyzer/types.ts`, `apps/web/src/lib/analyzer/warning-display.ts`, `apps/web/src/components/FallbackReport.tsx`, `apps/web/test/unit/lib/analyzer/verdict-citation-sanitization.test.ts`.
**Key decisions:** Added `enforceVerdictCitationIntegrity(...)` after phantom cleanup and before spread adjustment/validation in Stage 4, and again as the final `validateVerdicts(...)` return sanitation so post-validation repairs cannot reintroduce invalid citations. The guard is structural only: it removes missing IDs, claim-scope mismatches, challenge-invalid IDs, non-direct applicability citations, and explicit neutral `claimDirection` citations; it moves direct evidence to the correct bucket when existing LLM-assigned `claimDirection` shows a bucket mismatch. Repair normalization now also drops explicit neutral citations instead of preserving the original bucket. Added report-degrading `verdict_citation_integrity_guard` with `error` severity when sanitation collapses a decisive citation side, because that may alter confidence tier or verdict direction.
**Verification:** `npm -w apps/web exec vitest run test/unit/lib/analyzer/verdict-citation-sanitization.test.ts test/unit/lib/analyzer/warning-display.test.ts test/unit/lib/analyzer/verdict-stage.test.ts` passed (191 tests). `npm -w apps/web run build` passed; postbuild reseed reported 0 config/prompt changes.
**Open items:** No post-change live jobs were submitted because live-job discipline requires committing and refreshing services before evaluation. Full `npm test` had three runner queue/concurrency timeouts earlier in the task, but the same two runner test files passed when rerun in isolation.
**Warnings:** A first explorer review confirmed the insertion point and test edges. A second review attempt could not complete because the sub-agent hit a usage limit; the final patch received local diff review plus focused tests and build.
**For next agent:** If validating with live jobs, commit/restart first, then use the Captain-approved inputs already exercised in the monitoring run. Compare for disappearance of `verdict_grounding_issue` / `verdict_direction_issue` cases where invalid/non-direct IDs survive in final `supportingEvidenceIds` or `contradictingEvidenceIds`.
**Learnings:** Not appended to `Role_Learnings.md`; this was an implementation slice without a role-specific durable lesson.
