### 2026-06-18 | Senior Developer | Codex (GPT-5) | Analysis Improvement Strategy Reset
**Task:** After repeated pipeline-improvement attempts, deeply analyze the failure pattern and propose a new strategy for improving analysis quality.
**Files touched:** Created this handoff; appended the index row in `Docs/AGENTS/Agent_Outputs.md`. No source, prompt, UCM, or test files changed.
**Key decisions:** Strategy should reset from ad hoc prompt/code tuning to a measurement-first improvement program. Current evidence does not support another narrow prompt patch: after commit `0396ea47`, both rerun jobs scored out of band and top-level `UNVERIFIED`; population census remains roughly 8.2% hard-failure and 24.6% checkworthy-claim `UNVERIFIED`. Gemini and Claude reviews both agreed that the next work should establish Q-code/failure-cause attribution, reuse `qualityHealth` and `pipelineTelemetry`, and isolate stages before behavior changes. First behavior candidates remain source-reliability prompt ownership, input-policy schema alignment, generic source-native retrieval-language planning, and decisive current-primary-metric sufficiency, but D5/Stage4 sufficiency is high-risk and should wait for isolation/diagnostics.
**Open items:** Extend the zero-spend scorer or companion diagnostics to emit a current-build failure-mode table by Captain input: Q-HF1, Q-BE, Q-EV6, Q-EV_LANGUAGE, D5 sufficiency reason, verdict integrity, source acquisition/fetch, and prompt rollout/provenance. Add or use an isolated stage harness before changing D5/Stage4 sufficiency. Then ship low-risk ownership/schema fixes with safe tests and scorer checks. Only after that implement source-native retrieval planning and D5/Stage4 sufficiency improvements.
**Warnings:** Do not prune `claimboundary.prompt.md` or resurrect reverted prompt edits as the next move. Do not add country/topic/language keyword tables or any deterministic semantic routing. Do not run more paid live jobs before a committed, locally verified fix and runtime refresh. The next behavior edit in the Stage2/D5 chain must start with failed-attempt recovery logging from baseline `0396ea47`.
**For next agent:** Treat this as the execution order: (1) diagnostic attribution baseline over stored reports/current builds; (2) stage isolation for retrieval vs D5/Stage4; (3) source-reliability/input-policy ownership fixes; (4) generic retrieval-language planner; (5) measured D5/Stage4 sufficiency path; (6) selective prompt pruning only after measured safety. Use only Captain-defined inputs.
**Learnings:** No Role_Learnings update. Durable lesson is captured here: analysis quality work needs attribution and isolation before additional prompt/code mechanisms.
