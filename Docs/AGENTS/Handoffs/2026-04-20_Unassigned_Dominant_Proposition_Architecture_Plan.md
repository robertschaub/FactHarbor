---
### 2026-04-20 | Unassigned | Codex (GPT-5) | Dominant Proposition Architecture Plan
**Task:** Consolidate the reviewed solution and implementation plan for optional parent/top-level proposition support, then re-adjudicate it after external architecture/risk reviews.
**Files touched:** `Docs/WIP/2026-04-20_Dominant_Proposition_Architecture_Plan.md`; `Docs/WIP/README.md`; `Docs/AGENTS/Handoffs/2026-04-20_Unassigned_Dominant_Proposition_Architecture_Plan.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Keep the core model: optional parent outside `atomicClaims`, child-only research/verdicting, no persisted `keep | repair | drop`, and no parent in `claimVerdicts`. Tightened after review: Phase A is now detection-only by default; `topLevelPropositionAggregationEnabled` should default `false` until benchmark observation is complete. Dropped `evaluationMode` from the Phase A contract, added a requirement for post-retry `componentClaimIds` validation, made the `articleThesis` / `topLevelProposition` relationship explicit, and required prompt-level negative rules for alternative-dimension inputs such as `plastic-en`. If parent-aware aggregation ships later, it should use an explicit `all_must_hold` path, bypass `dominanceAssessment`, and record `aggregationMode` plus `constrainingClaimId` in the adjudication trail.
**Open items:** No code is implemented yet. The next execution step is Phase A only: add the field, validation, JSDoc/prompt clarifications, flags, and detection-only validation on approved benchmark families. Phase B should not start until detection quality is stable on the null-controls.
**Warnings:** `dominantProposition` remains a bad code name because Stage 5 already uses `dominanceAssessment`. Do not use deterministic heuristics over `claimWeightRationale` to infer parent semantics; if richer semantics than strict conjunction are needed later, add them explicitly. Do not let `topLevelProposition` become a loophole that allows whole-input restatements where no genuine conjunctive parent exists.
**For next agent:** Start from [2026-04-20_Dominant_Proposition_Architecture_Plan.md](/c:/DEV/FactHarbor/Docs/WIP/2026-04-20_Dominant_Proposition_Architecture_Plan.md). Use `topLevelProposition` in code, keep it outside `atomicClaims`, validate `componentClaimIds` against the final post-retry claim set, and treat `Plastic recycling is pointless` as a hard null-control. Keep the short Sonnet/Gemini review prompts from the plan doc if external review is requested again.
**Learnings:** External reviews materially improved the rollout shape. The architecture itself survived review, but the previous plan was too eager on aggregation and too loose about the `articleThesis` relationship, ID validation timing, and alternative-dimension false positives.
