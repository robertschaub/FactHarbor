---
### 2026-04-17 | Unassigned | Codex (GPT-5) | MT5C Contract-Approved Skip Fix
**Task:** Implement the Stage 1 forward fix from the `d816e2e8...` report review so `MT-5(C)` does not reprompt a contract-approved 1-claim set and reintroduce the Bundesrat `report_damaged` collapse.
**Files touched:** `apps/web/src/lib/analyzer/claim-extraction-stage.ts`; `apps/web/test/unit/lib/analyzer/claimboundary-pipeline.test.ts`; `Docs/AGENTS/Handoffs/2026-04-17_Unassigned_MT5C_Contract_Approved_Skip_Fix.md`; `Docs/AGENTS/Agent_Outputs.md`
**Key decisions:** Extended the existing `currentSetIsContractApproved` safeguard into the later `MT-5(C)` branch in `claim-extraction-stage.ts`. The generic reprompt loop already treated contract-approved sets as sacred; the bug was that the multi-event collapse guard still regenerated a repaired or otherwise contract-approved single-claim set solely because `distinctEvents >= 2`. Added a focused regression test proving that a contract-approved 1-claim set with two detected events stops after Gate 1 and does not enter the `MT-5(C)` reprompt path.
**Open items:** No live rerun was executed in this turn, so the code fix is verified by unit coverage and compile only. The benchmark metadata for `bundesrat-rechtskraftig` may still need follow-up because same-day history contains both healthy and failed runs.
**Warnings:** The local worktree already contains the earlier report-review handoff row/file from the investigation step. This fix does not clear or alter that prior review artifact. The new regression test covers the control-flow invariant on a contract-approved initial set; the real failing live jobs reached the same branch after repair, so a fresh rerun of the exact Captain-defined input remains the highest-value empirical follow-up.
**For next agent:** Start with `apps/web/src/lib/analyzer/claim-extraction-stage.ts:775-783` and `apps/web/test/unit/lib/analyzer/claimboundary-pipeline.test.ts:9891`. If you want runtime confirmation, rerun the exact Captain-defined Bundesrat input and compare its Stage 1 event sequence against prior failed jobs `d816e2e8abf14fa6a0c5f63a20e9b4a8` / `1d9d9389c98249c59bed17d4633a52cc`; the expected difference is that a contract-approved 1-claim set no longer emits `Extracting claims: multi-event reprompt...`.
**Learnings:** no — no `Role_Learnings.md` update needed.
